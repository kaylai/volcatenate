# Configuration Options Reference

The exhaustive reference for every YAML field volcatenate exposes — what it does, what values it accepts, and (for backend-specific fields) how it propagates into the underlying model. For the *mechanics* of building, loading, saving, and overriding a config, see [configuration.md](configuration.md).

**How do define sample properties and run settings**

- [What goes in sample inputs, what goes in the YAML config, what goes in both?](#what-goes-in-the-sample-inputs-what-goes-in-the-yaml-config-what-goes-in-both)

**Universal run settings**

- [Top-level YAML settings](#top-level-runconfig-settings) — the universal fields that apply to every run: output paths, logging, progress bars, run-bundle saving.

**Per-backend sections**, ordered from simplest to most involved:

1. [VESIcal](#vesical)
2. [VolFe](#volfe)
3. [EVo](#evo)
4. [MAGEC](#magec)
5. [SulfurX](#sulfurx)

**Redox handling** is very complicated! Per-tool conventions, fallback cascades, and how redox parameters interact with each other are discuss in detail:

- [Redox Handling](#redox-handling) — how each backend converts the chosen sample field into its internal fO2 representation, what equation each YAML key picks, and which other keys are read or ignored in each mode. Also see redox notes in per-backend sections and field-by-field tables.

**volcatenate conventions**:

- [Unit conventions](#unit-conventions)
- [Logging](#logging)
- [Output CSV schema](#output-csv-schema)

**Worked examples** at the end:

- [Worked examples — fO2 initialization](#worked-examples-fo2-initialization) — five common scenarios showing how each backend handles redox initialization side-by-side.

**Details on how each backend handles sulfur**:

- [Sulfur deep-dive](#sulfur-deep-dive-cross-backend-model-choices) — cross-backend mapping of S²⁻ / S⁶⁺ solubility, S6+/ST speciation, SCSS, SCAS, and saturation enforcement; ends with a recipe for running all four backends with the O'Neill family.

**Quick reference and details on internal model choice**

- [Quick cross-reference](#quick-cross-reference)

---

## What goes in the sample inputs, what goes in the YAML config, what goes in both?

You provide volcatenate two kinds of input:

**1. Sample data** — a description of one melt's composition, temperature, volatile content, and redox values. This is supplied by a pre-existing csv file or by a user-created `MeltComposition` or dict object (i.e., define sample information in a script, then run calculations).

**2. YAML config** that tells the wrapper which models to run and how to run them. Most fields belong cleanly to one or the other, but a handful of fields can live in either place, with rules about which wins. This section maps every field a new user will encounter so you know where each piece of information has to go.

If you haven't read [sample_data.md](sample_data.md) yet, that file walks through the three ways to supply sample data (CSV file, Python dict, `MeltComposition` instance). They are equivalent — the CSV reader and the dict path both produce `MeltComposition` instances internally, so any field listed below by its `MeltComposition` name is the same field as the matching CSV column or dict key.

```{mermaid}
flowchart LR
    subgraph SAMPLE["Sample data<br/>(CSV / dict / MeltComposition)"]
        S1["T_C, major oxides"]
        S2["H₂O, CO₂, S<br/>(volatile budget)"]
        S3["Redox: Fe3FeT / dNNO / dFMQ<br/>(any one is enough)"]
        S4["Optional: Cr2O3, N_ppm,<br/>Xppm, Reservoir"]
    end
    subgraph YAML["YAML config / RunConfig"]
        Y1["Solubility & equilibrium<br/>model selections"]
        Y2["Pressure stepping,<br/>gas system, run type"]
        Y3["Redox-source rules<br/>(which sample value to use)"]
        Y4["Output paths, logging,<br/>run-bundle settings"]
    end
    SAMPLE --> RUN["volcatenate run<br/>(EVo / VolFe / MAGEC / SulfurX / VESIcal)"]
    YAML --> RUN
    RUN --> OUT["Standardized CSVs<br/>+ run bundle JSON<br/>+ resolved-input YAMLs"]
```

### Sample inputs (CSV file, `MeltComposition`, or dict)

#### Required in sample input

| Field on the sample (CSV column / dict key /`MeltComposition` field)                | Used by | What it is                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `Sample` (alias `Label`)                                                          | All     | Identifier — used as the row key in output CSVs and as the per-sample override key in YAML.                                                                                                                                                                                                     |
| `T_C` (alias `Temp`, `Temperature`)                                             | All     | Temperature in °C. Each backend converts to Kelvin internally where needed.                                                                                                                                                                                                                     |
| `SiO2`, `TiO2`, `Al2O3`, `MnO`, `MgO`, `CaO`, `Na2O`, `K2O`, `P2O5` | All     | Major-oxide composition, all wt%. Each defaults to `0.0` in `MeltComposition` if you omit it, so volcatenate will not error — but every backend needs sensible values across the major-oxide set for the calculation to be physically meaningful. Treat these as required for any real run. |
| `FeOT` *or* speciated `FeO` + `Fe2O3`                                         | All     | Total iron, wt%. You can supply `FeOT` directly, or supply both `FeO` and `Fe2O3` and let the wrapper compute `FeOT` and `Fe3+/FeT` from them. Don't mix the two forms.                                                                                                                |
| `H2O`, `CO2`, `S`                                                               | All     | Bulk volatile budget, all wt%. Volcatenate rescales these per backend (some want ppm, some want mass fraction, some want wt% directly), but the underlying meaning is unchanged.                                                                                                                 |

These fields **cannot** appear in the YAML config. The YAML has no `H2O` or `T_C` setting — your samples define those, period.

One detail worth knowing: H₂O, CO₂, and S are taken in as **neutral oxides / elemental sulfur** (not as separately speciated melt species like H₂, CH₄, etc.). Whether a backend internally re-speciates them — splitting H₂O into H₂ and H₂Oₘₒₗ, or producing OCS / H₂S in the vapor — depends on the backend's own machinery and on its COH-species YAML setting (e.g. `volfe.coh_species`).

#### Optional data that can only be set in the sample inputs

These are sample fields some backends use and others ignore. The YAML has no equivalent — to include them in a run, they have to be on the sample.

| Field on the sample | Used by       | What it is                                                                       | Default if absent       |
| ------------------- | ------------- | -------------------------------------------------------------------------------- | ----------------------- |
| `Cr2O3`           | MAGEC only    | wt% Cr₂O₃, included in MAGEC's anhydrous renormalization                       | 0.0 (treated as absent) |
| `Xppm`            | VolFe only    | "Other" trace species (Ar or Ne; species identity is set by `volfe.species_x`) | 0.0                     |
| `Reservoir`       | none directly | Free-text grouping label propagated to output for plotting                       | empty string            |

### Nitrogen in EVo: The one field where the YAML can fill in for the sample

EVo's nitrogen handling is the only field where, when sample data is absent, a YAML value takes its place. When `evo.nitrogen_set: True`:

1. If the sample has `N_ppm > 0` (alias columns: `Nppm`, `Nitrogen`), EVo's `NITROGEN_START` is filled from that value (converted ppm → mass fraction).
2. Otherwise the wrapper uses `evo.nitrogen_start` from the YAML (default `0.0001` — a tiny seed).

---

## Top-level `RunConfig` Settings

These are the YAML fields that live at the top of the config file (not under any backend section). They control where output goes, how logging behaves, whether to save a reproducible run bundle, and a few related concerns that apply to every backend at once. The dataclass definition is [`RunConfig` in config.py](../src/volcatenate/config.py).

### Field-by-field

| YAML key            | Default               | What it does                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Where it lands                                                                                                                                                                          |
| ------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `output_dir`      | `"."`               | Root directory for**everything** the run produces — standardized result CSVs, the run bundle JSON, the per-`(sample, backend)` resolved-input YAML sidecars, and the `raw_output_dir` subtree. The path is taken as-is (relative to the cwd if not absolute) and created if it doesn't exist.                                                                                                                                                                                                                                                                 | Used by every entry point in[`core.py`](../src/volcatenate/core.py); plumbed through to each backend wrapper.                                                                            |
| `raw_output_dir`  | `"raw_tool_output"` | Subdirectory**under `output_dir`** where each backend drops its scratch files: EVo writes per-sample `chem.yaml` / `env.yaml` / `output.yaml` plus EVo's own CSV output; MAGEC writes the per-sample input/output xlsx pair; VolFe writes its captured stdout / log fragments; SulfurX writes nothing here today. The path is interpreted relative to `output_dir`, so the absolute scratch path is `<output_dir>/<raw_output_dir>/...`.                                                                                                               | Each backend builds `work_dir = os.path.join(config.output_dir, config.raw_output_dir, ...)` at the start of its calculate-* method.                                                  |
| `keep_raw_output` | `True`              | Whether to keep `raw_output_dir` on disk after the run. `True` retains every backend's scratch files for inspection. `False` deletes the `raw_output_dir` tree (per-sample subdirs first, then the top-level dir if empty) at the end of the run. The standardized result CSVs, the run bundle JSON, and the resolved-input sidecars are unaffected — they live outside `raw_output_dir`.                                                                                                                                                                     | Cleanup is per-backend after each calculate-* call (e.g.[evo.py:163](../src/volcatenate/backends/evo.py)) plus a final empty-dir sweep in [`run_comparison`](../src/volcatenate/core.py).   |
| `verbose`         | `False`             | Whether to send INFO-level log messages from the volcatenate logger to stdout. When `False` the logger is silent on the terminal (a `NullHandler` is installed unless `log_file` is also set). When `True`, a Rich-formatted handler is attached if Rich is available; falls back to a plain stdout handler otherwise. Has no effect on what gets written to `log_file`.                                                                                                                                                                                       | Consumed by[`setup_logging`](../src/volcatenate/log.py), called automatically by every entry point.                                                                                      |
| `log_file`        | `""`                | Path to a file that captures**all** DEBUG-level messages, including the per-step output that gets routed there from EVo's stdout, MAGEC's MATLAB stdout, and VESIcal's captured warnings. Empty string disables file logging. The file is **truncated on the first call within a Python process**, then **appended to** thereafter — so multiple `calculate_*` calls in the same notebook accumulate into one log instead of clobbering each other. To force a fresh log mid-session, call [`reset_log_file_tracking()`](../src/volcatenate/log.py). | [`setup_logging`](../src/volcatenate/log.py) attaches a `FileHandler`.                                                                                                                 |
| `show_progress`   | `True`              | Show Rich progress bars during multi-sample runs.`False` is the right choice for CI / headless / batch scripts where progress bars produce noisy output.                                                                                                                                                                                                                                                                                                                                                                                                               | Consumed by[`_init_progress`](../src/volcatenate/core.py); only matters when Rich is installed.                                                                                          |
| `save_bundle`     | `""`                | Path at which to write a reproducible JSON run bundle. Empty string disables. The bundle is written**twice**: an inputs-only version is saved up front (so the bundle survives a mid-run crash), and a full version with `resolved_inputs` populated from the run is saved at end of run. See [run_bundles.md](run_bundles.md) for the full anatomy.                                                                                                                                                                                                                | [`create_bundle`](../src/volcatenate/reproducible.py) + [`save_bundle`](../src/volcatenate/reproducible.py); orchestrated by every entry point in [`core.py`](../src/volcatenate/core.py). |
| `bundle_comments` | `""`                | Free-text notes recorded in the bundle's `comments` field. **Provenance only** — ignored on replay. Useful for "why this run exists," patches you applied to a backend, expected outcomes, etc. The `comments=` kwarg on [`create_bundle`](../src/volcatenate/reproducible.py) overrides this when both are supplied.                                                                                                                                                                                                                                          | Stored verbatim in the bundle JSON.                                                                                                                                                     |

### On-disk layout the top-level fields produce

A run with `output_dir="my_run"`, `raw_output_dir="raw_tool_output"`, `save_bundle="my_run/bundle.json"`, `keep_raw_output=True`, and a single sample called `Fuego` run through EVo + MAGEC produces this tree:

```text
my_run/
├── bundle.json                       # the run bundle (save_bundle)
├── saturation_pressures.csv          # per-(sample, model) sat pressures
├── EVo/
│   └── fuego.csv                     # standardized degassing path, EVo
├── MAGEC/
│   └── fuego.csv                     # standardized degassing path, MAGEC
├── resolved_inputs/                  # per-(sample, backend) actual model inputs
│   └── Fuego/
│       ├── EVo.yaml                  # what EVo's env.yaml + chem.yaml + output.yaml had
│       └── MAGEC.yaml                # what MAGEC's input row + settings struct had
└── raw_tool_output/                  # = raw_output_dir
    ├── Fuego_evo_satp/
    │   ├── env.yaml
    │   ├── chem.yaml
    │   ├── output.yaml
    │   └── output/                   # EVo's own native CSV output
    └── magec/
        └── Fuego/
            ├── Fuego_input.csv
            └── Fuego_output.csv
```

With `keep_raw_output=False`, the `raw_tool_output/` subtree is deleted at end of run; everything else (the bundle, the standardized CSVs, the `resolved_inputs/` sidecars) survives. The `resolved_inputs/` sidecars are **always** written when `save_bundle` is on, regardless of `keep_raw_output` — they are deliberately outside `raw_output_dir` so they survive a cleanup.

### Logging in practice

The `verbose` and `log_file` fields control two independent destinations:

- `verbose=True` → INFO and above to stdout (Rich-formatted if available).
- `log_file="path.log"` → DEBUG and above to the file.

Both can be on at once. Setting neither leaves the volcatenate logger silent.

What lands at DEBUG level (and therefore in the log file but not on the terminal) includes: EVo's full per-step stdout (captured via `contextlib.redirect_stdout`), MAGEC's MATLAB stdout, every VESIcal warning the wrapper captured (tagged `[VESIcal/<phase>/<sample>] <Category>: <msg>`), and the resolved fO2-source choice for every sample in the run.

### Reading the bundle

`save_bundle` produces a single JSON file that captures both the *intent* (what you put in the YAML and which compositions you ran) and the *realization* (what each backend actually handed to its underlying model, sample by sample, in `resolved_inputs`). See [run_bundles.md](run_bundles.md) for the field-by-field anatomy and replay semantics.

---

## VESIcal

VESIcal is the simplest backend — it models only H₂O–CO₂ degassing (no sulfur, no Fe redox), so the wrapper is short and the YAML surface is tiny.

### How the YAML propagates

```
volcatenate_config.yaml
  └── vesical:
       └── RunConfig.vesical (VESIcalConfig dataclass)
            └── Backend.calculate_degassing()
                 └── model.calculate_degassing_path()
```

Wrapper code: [backends/vesical.py](../src/volcatenate/backends/vesical.py).

The "VESIcal model" being used (Dixon, Iacono-Marziano, MagmaSat, …) is **not** controlled by the YAML — it's selected by the **backend name** you pass to `calculate_degassing` / `run_comparison`, e.g. `models=["VESIcal_Iacono"]`. See the `VARIANT_MAP` at [backends/vesical.py:28](../src/volcatenate/backends/vesical.py).

### Field-by-field

| YAML key              | What it does (plain English)                                                                                                                                                                         | Backend option                                           | Gotchas                                                                                       |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| `steps`             | How many pressure steps in the degassing path. More steps = smoother curves but slower.                                                                                                              | `model.calculate_degassing_path(steps=…)`             | VESIcal divides the (P_sat → final_pressure) range into this many steps. 101 is the default. |
| `final_pressure`    | The pressure (bar) at which the degassing run ends.                                                                                                                                                  | `model.calculate_degassing_path(final_pressure=…)`    | Set this to ~1 bar for full atmospheric degassing, or higher to stop early.                   |
| `fractionate_vapor` | A number between 0 and 1: what fraction of vapor is removed from the melt at each step.`0` = closed-system (vapor stays in equilibrium), `1` = open-system (vapor is fully extracted each step). | `model.calculate_degassing_path(fractionate_vapor=…)` | Intermediate values are physically unusual but allowed.                                       |
| `overrides`         | Per-sample dict of any of the above fields, e.g.`{Fogo: {steps: 50}}`.                                                                                                                             | (resolved in `resolve_sample_config`)                  | Unknown field names are warned and ignored.                                                   |

### Hidden behaviors

- **Variant selection**: the actual VESIcal solubility model is chosen by the *backend name*, not by config. E.g. `"VESIcal_Iacono"` → `"IaconoMarziano"`, `"VESIcal_MS"` → `"MagmaSat"`. See `VARIANT_MAP` at [backends/vesical.py:28](../src/volcatenate/backends/vesical.py).
- **Degassing always starts at saturation.** VESIcal's ``calculate_degassing_path`` accepts either ``pressure="saturation"`` (compute the starting P from the sample's H₂O+CO₂) or a numeric value (start at that fixed pressure instead). The volcatenate wrapper hardcodes ``"saturation"``; there is no YAML setting today that lets the user provide a numeric starting P. If you want to start a degassing path at a fixed P below saturation, you would need to call VESIcal directly or extend the wrapper.
- **Iron is sent both ways** ([backends/vesical.py:181-186](../src/volcatenate/backends/vesical.py)): if the sample provides speciated `FeO` and `Fe2O3`, both are sent; otherwise FeOT is sent as `FeO`. This matters only for MagmaSat, which uses Fe redox internally.
- **MagmaSat requires Thermoengine.** `VESIcal_MS` resolves to VESIcal's `MagmaSat` model, which depends on the optional `thermoengine` package. If Thermoengine is not installed in the active environment, requesting `VESIcal_MS` will raise a `KeyError` at runtime when looking up `default_models["MagmaSat"]`. The four other variants (`VESIcal_Iacono`, `VESIcal_Dixon`, `VESIcal_Liu`, `VESIcal_ShishkinaIdealMixing`) work with a stock VESIcal install.
- **Warnings are captured and routed to the log file** ([backends/vesical.py](../src/volcatenate/backends/vesical.py)). VESIcal emits many petrological-range warnings (e.g. composition outside the calibration band, T outside the tested range). Volcatenate suppresses them on the terminal so logs stay readable, but if you set ``log_file`` in your ``RunConfig`` they are forwarded at DEBUG level and tagged ``[VESIcal/<phase>/<sample>]``, so you can audit them after a run.

### Fallback chains

VESIcal does not use any of the redox indicators (`Fe3FeT`, `dFMQ`, `dNNO`) — it doesn't model fO2 except internally for MagmaSat. So there is no fallback chain to document.

---

## VolFe

VolFe is a Python C-O-H-S-Fe degassing model. The user's volcatenate YAML and sample inputs (CSV / `MeltComposition` / dict) are translated into two pandas DataFrames that VolFe consumes: a `setup_df` carrying sample composition + the chosen redox indicator, and a `models_df` enumerating every model-option toggle. Volcatenate then calls `vf.calc_gassing(setup_df, models=models_df)` for degassing runs (or `vf.calc_Pvsat(setup_df, models=models_df)` for saturation-pressure runs). VolFe writes progress to stdout/stderr and a few debug CSVs into the current working directory; the wrapper's `_quiet_volfe()` context redirects stdout/stderr into the volcatenate logger at DEBUG and isolates the debug CSVs to a per-sample work directory.

### Files saved per VolFe run

For one VolFe run on a sample called `Fuego`, the on-disk layout produced is:

```
<output_dir>/
├── VolFe/
│   └── fuego.csv                              # standardized degassing path (always)
├── saturation_pressures.csv                   # row for each (sample, model) (when running calculate_saturation_pressure)
├── resolved_inputs/
│   └── Fuego/
│       └── VolFe.yaml                         # exact setup + models dicts the wrapper handed VolFe (when save_bundle is on)
├── <save_bundle>.json                         # full run bundle including resolved_inputs (when save_bundle is set)
└── <raw_output_dir>/                          # = "raw_tool_output" by default
    └── Fuego_volfe_degas/                     # (or _volfe_satp for sat-pressure calls)
        ├── results_jacnewton2.csv             # only when keep_raw_output: true
        └── results_jacnewton3.csv             # only when keep_raw_output: true
```

When `keep_raw_output: false`, the entire `<raw_output_dir>/Fuego_volfe_degas/` subtree is deleted at the end of the run. The standardized degassing CSV (`VolFe/fuego.csv`), the `resolved_inputs/Fuego/VolFe.yaml` sidecar, and the run bundle JSON are unaffected by `keep_raw_output` — they always survive. VolFe's own native CSV output is suppressed unconditionally because the wrapper sets the `output csv` model option to `"False"`; only the two `jac_newton`-step debug CSVs land in the work directory.

The same on-disk layout is documented in the [Top-level RunConfig settings](#on-disk-layout-the-top-level-fields-produce) section above; the file list here is the VolFe-specific subset.

### How the YAML propagates

```
volcatenate_config.yaml
  └── volfe:
       └── RunConfig.volfe (VolFeConfig dataclass)
            ├── _build_setup_df()    → sample/composition + fO2 indicator
            └── _build_models_df()   → all the model-option toggles
                 └── vf.calc_gassing(setup_df, models=models_df)
```

Wrapper code: [backends/volfe.py](../src/volcatenate/backends/volfe.py).

### Field-by-field — VolFe

The "VolFe option" column gives the exact string used as the index value of VolFe's `models_df` — the table VolFe actually reads. Defaults are explicit. Allowed values are enumerated when finite; for continuous fields the column gives a Python type plus a bound. Where a string field's accepted values are pre-publication or in-prep, that is called out in the cell.

| volcatenate YAML key                                                                              | Default                | Allowed values (with type)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Behavior                                                                                                                                                                                                                                                                                                                                                                                                        | VolFe option                            |
| ------------------------------------------------------------------------------------------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| `fo2_column`                                                                                    | `Fe3FeT`             | `str`: `Fe3FeT`, `DNNO`, `DFMQ`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Volcatenate wrapper setting (no native VolFe equivalent). Names which redox column the wrapper writes into VolFe's `setup_df`. See the expanded `volfe.fo2_column` and `volfe.fo2_source` subsection below.                                                                                                                                                                                               | (drives `_build_setup_df`)            |
| `fo2_source`                                                                                    | `auto`               | `str`: `auto`, `fe3fet`, `dnno`, `dfmq`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Volcatenate wrapper setting (no native VolFe equivalent). Strictness of the choice above. See the expanded subsection below.                                                                                                                                                                                                                                                                                    | (drives `_resolve_volfe_redox`)       |
| `sulfur_saturation`                                                                             | `false`              | `bool`: `true`, `false`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | When `true`, VolFe checks for sulfide / sulfate saturation at every pressure step and clips dissolved S to the SCSS / SCAS value when it would exceed it. When `false`, bulk S is treated as a soluble component throughout. The wrapper passes the value as a string (`"True"` / `"False"`) because VolFe's `models_df` stores all options as strings.                                               | `sulfur_saturation`                   |
| `graphite_saturation`                                                                           | `false`              | `bool`: `true`, `false`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Analogous to `sulfur_saturation` but for graphite — clips melt CO / CH4 if a graphite-saturation criterion is reached. Most basaltic systems are not graphite-saturated.                                                                                                                                                                                                                                     | `graphite_saturation`                 |
| `sulfur_is_sat`                                                                                 | `no`                 | `str`: `yes`, `no`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | When `yes`, the melt is treated as already sulfur-saturated at the starting pressure. When `no`, saturation (if it occurs) is reached only as P drops.                                                                                                                                                                                                                                                      | `sulfur_is_sat`                       |
| `gassing_style`                                                                                 | `closed`             | `str`: `closed`, `open`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `closed` keeps the exsolved vapor in equilibrium with the melt at every step; `open` removes the vapor instantly each step. Open-system runs produce lower vapor S/C ratios as light volatiles escape early.                                                                                                                                                                                                | `gassing_style`                       |
| `gassing_direction`                                                                             | `degas`              | `str`: `degas`, `regas`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `degas` runs P from saturation downward (normal eruptive path). `regas` runs upward — recompression diagnostic.                                                                                                                                                                                                                                                                                            | `gassing_direction`                   |
| `bulk_composition`                                                                              | `melt-only`          | `str`: `melt-only`, `melt+vapor_wtg`, `melt+vapor_initialCO2`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | `melt-only` = the sample melt, with vapor appearing as P drops. `melt+vapor_wtg` = melt + a fixed weight fraction of pre-existing vapor. `melt+vapor_initialCO2` = melt + vapor that holds a specific initial CO2.                                                                                                                                                                                        | `bulk_composition`                    |
| `starting_p`                                                                                    | `Pvsat`              | `str`: `Pvsat`, `set`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `Pvsat` starts the run at the saturation pressure VolFe finds; `set` switches VolFe into a user-supplied-starting-P mode. Volcatenate does not currently expose a starting-P value through the YAML, so `set` falls through to VolFe's own internal default.                                                                                                                                              | `starting_P`                          |
| `p_variation`                                                                                   | `polybaric`          | `str`: `polybaric`, `isobaric`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `polybaric` = P varies through the run (normal degassing). `isobaric` = P held fixed.                                                                                                                                                                                                                                                                                                                       | `P_variation`                         |
| `t_variation`                                                                                   | `isothermal`         | `str`: `isothermal`, `polythermal`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `isothermal` = T held fixed; `polythermal` = T varies during the run.                                                                                                                                                                                                                                                                                                                                       | `T_variation`                         |
| `crystallisation`                                                                               | `no`                 | `str`: `no`, `yes`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | When `yes`, track crystallization during degassing.                                                                                                                                                                                                                                                                                                                                                           | `crystallisation`                     |
| `eq_fe`                                                                                         | `yes`                | `str`: `yes`, `no`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | When `yes`, VolFe iterates Fe redox equilibrium with fO2 at every pressure step (fO2 is recomputed from `mdv.f_O2` and Fe3+/FeT is re-derived each step inside `eq_Fe_yes` in `VolFe/calculations.py`). When `no`, Fe is frozen (`wt_Fe = 0` is set internally), decoupling iron from gas-phase chemistry; only useful for diagnostics.                                                             | `eq_Fe`                               |
| `bulk_o`                                                                                        | `exc_S`              | `str`: `exc_S`, `inc_S`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `exc_S` excludes sulfur-bound oxygen from bulk-O accounting; `inc_S` includes it. Affects how O budgets are tracked, not the gas-melt thermodynamic equilibrium.                                                                                                                                                                                                                                            | `bulk_O`                              |
| `calc_sat`                                                                                      | `fO2_melt`           | `str`: `fO2_melt`, `fO2_fX`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Saturation-pressure search mode.`fO2_melt` drives the satP root-find via melt fO2; `fO2_fX` uses the species-X fugacity instead.                                                                                                                                                                                                                                                                            | `calc_sat`                            |
| `coh_species`                                                                                   | `yes_H2_CO_CH4_melt` | `str`: `yes_H2_CO_CH4_melt`, `no_H2_CO_CH4_melt`, `H2O-CO2 only`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Which C-O-H species VolFe carries in melt and vapor.`yes_H2_CO_CH4_melt` = full speciation including H2, CO, CH4 dissolved in the melt. `no_H2_CO_CH4_melt` = only CO2 / H2O in the melt (others only in vapor). `H2O-CO2 only` = strictly two-component. Affects redox-sensitive volatile speciation, especially at reduced fO2.                                                                         | `COH_species`                         |
| `h2s_melt`                                                                                      | `true`               | `bool`: `true`, `false`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | When `true`, treats H2Smol as a dissolved melt species (not just vapor). Generally `true` for proper sulfur mass balance below ~FMQ. The wrapper passes the value as a string (`"True"` / `"False"`).                                                                                                                                                                                                   | `H2S_m`                               |
| `species_x`                                                                                     | `Ar`                 | `str`: `Ar`, `Ne`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Identity of the inert "X" trace species. Only used if the sample carries a non-zero `Xppm`. Default sample data has `Xppm = 0`, so this is moot unless supplied.                                                                                                                                                                                                                                            | `species X`                           |
| `h_speciation`                                                                                  | `none`               | `str`: `none` (only option recognized at present)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | H-melt speciation submodel. Kept here so users can see it exists; only `none` is recognized in upstream VolFe today.                                                                                                                                                                                                                                                                                          | `Hspeciation`                         |
| `fo2_model`                                                                                     | `Kress91A`           | `str`: `Kress91A` (Kress & Carmichael (1991) CMP 108:82-92, eq. A-5/A-6), `Kress91` (Kress & Carmichael (1991) CMP 108:82-92, eq. 7), `ONeill18` (O'Neill et al. (2018) EPSL 504:152-162, eq. 9a), `Borisov18` (Borisov et al. (2018) CMP 173:98, eq. 4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | The fO2 ↔ Fe3+/FeT relationship VolFe uses internally. Read every time VolFe needs to translate between Fe3+/FeT and fO2 — i.e., during initialization on the Fe3+/FeT path, and at every pressure step when `eq_fe = "yes"`. Differences between options are typically ~0.1 log units for basalts.                                                                                                         | `fO2`                                 |
| `fmq_buffer`                                                                                    | `Frost91`            | `str`: `Frost91` (Frost (1991) RiMG 25, ch. 1), `ONeill87` (O'Neill (1987) AmMin 72(1-2):67-75)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Definition of the FMQ buffer. Read whenever VolFe needs to convert between absolute fO2 and `dFMQ` — both during initialization on the dFMQ path and at every pressure step when reporting `dFMQ`. `Frost91` is what most modern data are reported relative to.                                                                                                                                          | `FMQbuffer`                           |
| `nno_buffer`                                                                                    | `Frost91`            | `str`: `Frost91` (Frost (1991) RiMG 25, ch. 1) (only option recognized at present)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Definition of the NNO buffer. Read whenever VolFe needs to convert between absolute fO2 and `dNNO`.                                                                                                                                                                                                                                                                                                           | `NNObuffer`                           |
| `density`                                                                                       | `DensityX`           | `str`: `DensityX` (Iacovino & Till (2019) Volcanica 2(1):1-10) (only option recognized at present)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Melt density model used for mass ↔ volume bookkeeping inside VolFe. Does not affect intensive quantities like fO2 or species mole fractions.                                                                                                                                                                                                                                                                   | `density`                             |
| `melt_composition`                                                                              | `Basalt`             | `str`: `Basalt`, `Rhyolite`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Melt-composition family for parameterizations. Selecting `Rhyolite` swaps several upstream defaults (the rhyolite branch of the `default_models` table substitutes `Rhyolite_Blank93`, `Rhyolite_Hughes25`, `Andesite_Hughes24`, etc.). Volcatenate sends explicit values for each solubility / speciation field, so this setting only matters for fields the user leaves at their own VolFe-default. | `melt composition`                    |
| `co2_sol`                                                                                       | `MORB_Dixon95`       | `str`: see "Per-composition `co2_sol` parameters" subsection below for the full list                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | CO2 (CO2mol + CO3²⁻) solubility constant. Often the dominant driver of inter-model disagreement in CO2 partitioning at saturation. Each option is calibrated for a specific melt composition family.                                                                                                                                                                                                          | `carbon dioxide`                      |
| `h2o_sol`                                                                                       | `Basalt_Hughes24`    | `str`: `Basalt_Hughes24` (Hughes et al. (2024) AmMin 109(3):422-438 — basalt H2O solubility, Fig. S2 from data compilation in Allison et al. (2022) for basalts with H2O < 6 wt%), `Rhyolite_Hughes25` (Hughes et al. (2025) Volcanica 8(2):457-481 — rhyolite, eq. S1 from data in Blank et al. (1993))                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | H2O solubility constant.                                                                                                                                                                                                                                                                                                                                                                                        | `water`                               |
| `h2_sol`                                                                                        | `Basalt_Hughes24`    | `str`: `Basalt_Hughes24` (Hughes et al. (2024) AmMin 109(3):422-438, Table S4 — basalt H2 solubility from Hirschmann et al. (2012) data), `Andesite_Hughes24` (Hughes et al. (2024) AmMin 109(3):422-438, Table S4 — andesite H2 solubility from Hirschmann et al. (2012) data)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | H2 solubility constant. Has effect only at reduced fO2 where dissolved H2 is non-trivial.                                                                                                                                                                                                                                                                                                                       | `hydrogen`                            |
| `sulfide_sol`                                                                                   | `ONeill21dil`        | `str`: `ONeill21dil` (O'Neill (2021) "Magma Redox Geochemistry" ch. 10, eq. 10.34 inc. H2O dilution), `ONeill21` (eq. 10.34 ex. H2O dilution), `ONeill21hyd` (eq. 10.34 + 10.49, hydrous), `Boulliung23_eq6` (Boulliung & Wood (2023) CMP 178:56, eq. 6), `Boulliung23_eq7` (eq. 7), `Boulliung23_eq7_12` (eq. 7 + eq. 12 P-effect), `GorojovskyPP_eq8` (Gorojovsky & Wood, pre-print, EarthArXiv, eq. 8), `GorojovskyPP_eq9` (pre-print eq. 9), `Thomas26_eq15` (Thomas & Wood (2026) GCA, eq. 15)                                                                                                                                                                                                                                                                                  | S²⁻ (sulfide) solubility constant — controls how much sulfide the melt holds at given fO2 and composition. Affects S degassing under reducing conditions.                                                                                                                                                                                                                                                    | `sulfide`                             |
| `sulfate_sol`                                                                                   | `ONeill22dil`        | `str`: `ONeill22dil` (O'Neill & Mavrogenes (2022) GCA 334:368-382, eq. 12a inc. H2O dilution), `ONeill22` (eq. 12a ex. H2O dilution), `Boulliung22nP` (Boulliung & Wood (2023) GCA 343:420, eq. 5, no P-term), `Boulliung22wP` (eq. 5 + Boulliung & Wood (2022) GCA 336:150-164 eq. 8 for P), `Boulliung23_eq9` (Boulliung & Wood (2023) CMP 178:56, eq. 9), `Boulliung23_eq9_12` (eq. 9 + eq. 12 P-effect), `Boulliung23_eq11` (eq. 11), `GorojovskyPP_eq10` (Gorojovsky & Wood, pre-print, eq. 10), `GorojovskyPP_eq11` (pre-print eq. 11), `Thomas26_eq21` (Thomas & Wood (2026) GCA, eq. 21), `Thomas26_eq22` (eq. 22)                                                                                                                                                       | S⁶⁺ (sulfate) solubility constant. Affects S degassing under oxidizing conditions.                                                                                                                                                                                                                                                                                                                            | `sulfate`                             |
| `h2s_sol`                                                                                       | `Basalt_Hughes24`    | `str`: `Basalt_Hughes24` (Hughes et al. (2024) AmMin 109(3):422-438, Fig. S6 — basalt fitted to Moune et al. (2009) and Lesne et al. (2011)), `BasalticAndesite_Hughes24` (Hughes et al. (2024) AmMin 109(3):422-438, Fig. S6 — basaltic andesite fitted to same data)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | H2S solubility constant.                                                                                                                                                                                                                                                                                                                                                                                        | `hydrogen sulfide`                    |
| `ch4_sol`                                                                                       | `Basalt_Ardia13`     | `str`: `Basalt_Ardia13` (Ardia et al. (2013) GCA 114:52-71, eq. 7a) (only option recognized at present)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | CH4 solubility constant. Has effect only at strongly reduced fO2 (≲ FMQ−2).                                                                                                                                                                                                                                                                                                                                   | `methane`                             |
| `co_sol`                                                                                        | `Basalt_Hughes24`    | `str`: `Basalt_Hughes24` (Hughes et al. (2024) AmMin 109(3):422-438, Table S4 — basalt CO from Armstrong et al. (2015), Stanley et al. (2014), and Wetzel et al. (2013) data) (only option recognized at present)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | CO solubility constant. Has effect only at reduced fO2 where dissolved CO is non-trivial.                                                                                                                                                                                                                                                                                                                       | `carbon monoxide`                     |
| `x_sol`                                                                                         | `Ar_Basalt_Hughes25` | `str`: `Ar_Basalt_Hughes25` (Hughes et al. (2025) Volcanica 8(2):457-481, eq. S2 — Ar in basalt from Iacono-Marziano et al. (2010) data), `Ar_Rhyolite_Hughes25` (eq. S3 — Ar in rhyolite), `Ne_Basalt_Hughes25` (eq. S4 — Ne in basalt), `Ne_Rhyolite_Hughes25` (eq. S5 — Ne in rhyolite); also accepts a `float` literal for a constant value                                                                                                                                                                                                                                                                                                                                                                                                                                          | Inert species (Ar / Ne) solubility constant. All Hughes25 fits assume the species is an ideal gas (`y_X = 1`).                                                                                                                                                                                                                                                                                                | `species X solubility`                |
| `c_spec_comp`                                                                                   | `Basalt`             | `str`: `Basalt` (assume all oxidised carbon in the melt is CO3²⁻), `Andesite_Botcharnikov06` (Botcharnikov et al. (2006) Chem. Geol. 229:125-143, eq. 8), `Dacite_Botcharnikov06` (in-text eq., based on Behrens et al. (2004)), `Rhyolite` (assume all oxidised carbon is molecular CO2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | C-speciation composition: how oxidized C is split between molecular CO2 and CO3²⁻ in the melt.                                                                                                                                                                                                                                                                                                                | `Cspeccomp`                           |
| `h_spec_comp`                                                                                   | `MORB_HughesIP`      | `str`: `MORB_HughesIP` (Hughes et al. *in prep* — based on Dixon et al. (1995) data; no peer-reviewed citation yet), `StromboliAlkaliBasalt_Lesne10` (Lesne et al. (2010) CMP 162:133-151, eq. 15), `VesuviusAlkaliBasalt_Lesne10` (eq. 16), `EtnaAlkaliBasalt_Lesne10` (eq. 17), `Andesite_Botcharnikov06` (Botcharnikov et al. (2006) Chem. Geol. 229:125-143, eq. 7), `Albite_Silver89` (Silver & Stolper (1989) JPet 30(3):667-709, Fig. 8), `Rhyolite_Zhang97` (Zhang et al. (1997) GCA 61(15):3089-3100, eq. 9)                                                                                                                                                                                                                                                                 | H-speciation composition: how dissolved H is split between H2Omol and OH⁻ in the melt. The `MORB_HughesIP` default cites a Hughes et al. in-prep paper, so the citation is provisional.                                                                                                                                                                                                                      | `Hspeccomp`                           |
| `scss`                                                                                          | `ONeill21hyd`        | `str`: `ONeill21hyd` (O'Neill (2021) "Magma Redox Geochemistry" ch. 10, eqs. 10.34, 10.43, 10.45, 10.46, 10.49 — hydrous), `ONeill21` (same source, no water dilution), `ONeill21dil` (with water dilution), `Liu07` (Liu et al. (2007) GCA 71:1783-1799, eq. 9), `Fortin15_pss` (Fortin et al. (2015) eq. 7 via PySulfSat — Wieser & Gleeson (2023) Volcanica 6(1):107-127), `Liu21_pss` (Liu et al. (2021) eq. 2 via PySulfSat), `ONeill22_pss` (O'Neill & Mavrogenes (2022) via PySulfSat), `ONeill21_pss` (O'Neill (2021) via PySulfSat), `Smythe17_pss` (Smythe et al. (2017) via PySulfSat), `Li22_pss` (Li & Zhang (2022) eq. 19 via PySulfSat), `Blanchard21_eq11_pss` (Blanchard et al. (2021) eq. 11 via PySulfSat), `Blanchard21_eq12_pss` (eq. 12 via PySulfSat) | Sulfide content at sulfide saturation (SCSS). Read only when `sulfur_saturation = true`. The `_pss` suffix denotes options that route through PySulfSat (Wieser & Gleeson 2023).                                                                                                                                                                                                                            | `SCSS`                                |
| `scas`                                                                                          | `Zajacz19_pss`       | `str`: `Zajacz19_pss` (Zajacz & Tsay (2019) via PySulfSat — Wieser & Gleeson (2023) Volcanica 6(1):107-127), `Liu23` (Liu et al. (2023) GCA 349:135-145, eq. 4), `Chowdhury19_pss` (Chowdhury & Dasgupta (2019) via PySulfSat), `Masotta15_pss` (Masotta & Keppler (2015) via PySulfSat)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Sulfate content at anhydrite saturation (SCAS). Read only when `sulfur_saturation = true`.                                                                                                                                                                                                                                                                                                                    | `SCAS`                                |
| `ideal_gas`                                                                                     | `false`              | `bool`: `true`, `false`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | When `true`, all fugacity coefficients (y_*) are forced to 1 (vapor treated as ideal). Useful as a low-pressure sanity check; spurious at high P. The wrapper passes the value as a string.                                                                                                                                                                                                                   | `ideal_gas`                           |
| `y_co2`                                                                                         | `Shi92`              | `str`: `Shi92` (Shi & Saxena (1992) AmMin 77(9-10):1038-1049), `Holland91_eq8_tab1` (Holland & Powell (1991) CMP 109:265-273, eq. 8 + Table 1), `Holland91_eq4,A1-3_tab1` (eq. 4 + A1-A3 + Table 1), `Holland91_eq8,9_tab2` (eq. 8 + 9 + Table 2), `Flowers79` (Flowers (1979) — via MIMiC port from VolatileCalc / Newman & Lowenstern (2001)), `ideal` (force y = 1)                                                                                                                                                                                                                                                                                                                                                                                                                    | CO2 fugacity-coefficient model.`ideal_gas = true` overrides this.                                                                                                                                                                                                                                                                                                                                             | `y_CO2`                               |
| `y_so2`                                                                                         | `Shi92_Hughes23`     | `str`: `Shi92_Hughes23` (Hughes et al. (2023) JGSL 180(3) — Fig. S1, modified Shi & Saxena (1992)), `Shi92` (Shi & Saxena (1992) AmMin 77(9-10):1038-1049), `ideal` (force y = 1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | SO2 fugacity-coefficient model.                                                                                                                                                                                                                                                                                                                                                                                 | `y_SO2`                               |
| `y_h2s`                                                                                         | `Shi92_Hughes24`     | `str`: `Shi92_Hughes23` and `Shi92_Hughes24` are aliases — the upstream code accepts the latter and uses Hughes et al. (2024) AmMin 109(3):422-438 Fig. S1 modified from Shi & Saxena (1992); `Shi92` (Shi & Saxena (1992) AmMin 77(9-10):1038-1049); `ideal` (force y = 1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | H2S fugacity-coefficient model.                                                                                                                                                                                                                                                                                                                                                                                 | `y_H2S`                               |
| `y_h2`                                                                                          | `Shaw64`             | `str`: `Shaw64` (Shaw & Wones (1964) AmJSci 262:918-929, eq. 4), `ideal` (force y = 1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | H2 fugacity-coefficient model.                                                                                                                                                                                                                                                                                                                                                                                  | `y_H2`                                |
| `y_o2`                                                                                          | `Shi92`              | `str`: `Shi92` (Shi & Saxena (1992) AmMin 77(9-10):1038-1049), `ideal` (force y = 1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | O2 fugacity-coefficient model.                                                                                                                                                                                                                                                                                                                                                                                  | `y_O2`                                |
| `y_s2`                                                                                          | `Shi92`              | `str`: `Shi92`, `ideal` (force y = 1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | S2 fugacity-coefficient model.                                                                                                                                                                                                                                                                                                                                                                                  | `y_S2`                                |
| `y_co`                                                                                          | `Shi92`              | `str`: `Shi92`, `ideal` (force y = 1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | CO fugacity-coefficient model.                                                                                                                                                                                                                                                                                                                                                                                  | `y_CO`                                |
| `y_ch4`                                                                                         | `Shi92`              | `str`: `Shi92`, `ideal` (force y = 1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | CH4 fugacity-coefficient model.                                                                                                                                                                                                                                                                                                                                                                                 | `y_CH4`                               |
| `y_h2o`                                                                                         | `Holland91`          | `str`: `Holland91` (Holland & Powell (1991) CMP 109:265-273, eq. 4 + 6 + A1-A3 + Table 1, T > 673 K), `Flowers79` (Flowers (1979) via MIMiC), `ideal` (force y = 1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | H2O fugacity-coefficient model.                                                                                                                                                                                                                                                                                                                                                                                 | `y_H2O`                               |
| `y_ocs`                                                                                         | `Shi92`              | `str`: `Shi92`, `ideal` (force y = 1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | OCS fugacity-coefficient model.                                                                                                                                                                                                                                                                                                                                                                                 | `y_OCS`                               |
| `y_x`                                                                                           | `ideal`              | `str`: `ideal` (only option recognized at present)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Species-X fugacity-coefficient model.                                                                                                                                                                                                                                                                                                                                                                           | `y_X`                                 |
| `k_hog`                                                                                         | `Ohmoto97`           | `str`: `Ohmoto97` (Ohmoto & Kerrick (1997), reaction (d) in Table 1) (only option recognized at present)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Equilibrium constant for H2 + ½O2 = H2O.                                                                                                                                                                                                                                                                                                                                                                       | `KHOg`                                |
| `k_hosg`                                                                                        | `Ohmoto97`           | `str`: `Ohmoto97` (Ohmoto & Kerrick (1997), reaction (h)), `noH2S` (forces K = 0, suppresses H2S in vapor)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Equilibrium constant for ½S2 + H2O = H2S + ½O2.                                                                                                                                                                                                                                                                                                                                                               | `KHOSg`                               |
| `k_osg`                                                                                         | `Ohmoto97`           | `str`: `Ohmoto97` (Ohmoto & Kerrick (1997), reaction (f)), `noSO2` (forces K = 0, suppresses SO2)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Equilibrium constant for ½S2 + O2 = SO2.                                                                                                                                                                                                                                                                                                                                                                       | `KOSg`                                |
| `k_osg2`                                                                                        | `ONeill22`           | `str`: `ONeill22` (O'Neill & Mavrogenes (2022) GCA 334:368-382, eq. 6b) (only option recognized at present)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Equilibrium constant for the sulfate equilibrium (SO2 + ½O2 = SO3 / SO4²⁻).                                                                                                                                                                                                                                                                                                                                  | `KOSg2`                               |
| `k_cog`                                                                                         | `Ohmoto97`           | `str`: `Ohmoto97` (Ohmoto & Kerrick (1997), reaction (c)) (only option recognized at present)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Equilibrium constant for CO + ½O2 = CO2.                                                                                                                                                                                                                                                                                                                                                                       | `KCOg`                                |
| `k_cohg`                                                                                        | `Ohmoto97`           | `str`: `Ohmoto97` (Ohmoto & Kerrick (1997), reaction (e)), `noCH4` (forces K to a very large value, suppresses CH4)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Equilibrium constant for CH4 + 2 O2 = CO2 + 2 H2O.                                                                                                                                                                                                                                                                                                                                                              | `KCOHg`                               |
| `k_ocsg`                                                                                        | `Moussallam19`       | `str`: `Moussallam19` (Moussallam et al. (2019) EPSL, eq. 8), `noOCS` (forces K to a very large value, suppresses OCS)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | OCS-equilibrium constant.                                                                                                                                                                                                                                                                                                                                                                                       | `KOCSg`                               |
| `k_cos`                                                                                         | `Holloway92`         | `str`: `Holloway92` (Holloway et al. (1992) Eur. J. Mineral. 4:105-114, eq. 3 K1) (only option recognized at present)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Carbonate-solubility equilibrium constant for the melt-side CO2 ↔ CO3²⁻ reaction.                                                                                                                                                                                                                                                                                                                            | `KCOs`                                |
| `carbonylsulfide`                                                                               | `COS`                | `str`: `COS` (only option recognized at present)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Name of the OCS species in VolFe's vapor list.                                                                                                                                                                                                                                                                                                                                                                  | `carbonylsulfide`                     |
| `isotopes`                                                                                      | `no`                 | `str`: `no`, `yes`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | When `yes`, activates the isotope solver (then the `alpha_*` and `beta_factors` fields below are read). When `no`, those fields are sent to VolFe but never read.                                                                                                                                                                                                                                       | `isotopes`                            |
| `beta_factors`, `alpha_h_*`, `alpha_c_*`, `alpha_s_*`, `alpha_so2_so4`, `alpha_h2s_s` | (see config.py)        | `str`: model identifiers (e.g. `Rust04`, `Lee24`, `Fiege15`, `Blank93`, `no fractionation`); see VolFe's `model_dependent_variables.py` for the per-α option list                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Beta factors and isotope α-model identifiers. Only consulted when `isotopes = "yes"`. The full per-α option list is enumerated in upstream `model_dependent_variables.py` (search for `Model options for "alpha_*"`); volcatenate sends each as-is.                                                                                                                                                     | `beta_factors`, `alpha_*`           |
| `error`                                                                                         | `0.1`                | `float`, `> 0`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Numerical tolerance for the solver.                                                                                                                                                                                                                                                                                                                                                                             | `error`                               |
| `high_precision`                                                                                | `false`              | `bool`: `true`, `false`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | When `true`, VolFe runs in slower-but-tighter precision mode (uses `gmpy2` arbitrary-precision arithmetic in the inner loops). The wrapper passes the value as a string.                                                                                                                                                                                                                                    | `high precision`                      |
| `overrides`                                                                                     | `{}`                 | `dict[str, dict[str, Any]]`. Example: `{"Fogo": {"gassing_style": "open", "scss": "Liu07"}}`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Per-sample overrides keyed by sample name; values override the corresponding fields above for that sample only. Unknown field names are warned and ignored. See[Per-sample overrides in configuration.md](configuration.md#per-sample-overrides).                                                                                                                                                                  | (resolved in `resolve_sample_config`) |

### Per-composition `co2_sol` parameters

VolFe's CO2 solubility constant uses a Stolper & Holloway (1988)–style thermodynamic framework whose parameters are calibrated per melt composition. The full set of recognized strings:

| `volfe.co2_sol` value           | Calibrated for                                       | Source                                                                                      |
| --------------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| `MORB_Dixon95` (default)        | MORB and similar tholeiitic basalts                  | Dixon et al. (1995) JPet 36(6):1607-1631                                                    |
| `Basalt_Dixon97`                | Generic basalt                                       | Dixon (1997) AmMin 82(3-4):368-378, eq. 7                                                   |
| `NorthArchBasalt_Dixon97`       | North Arch alkalic basalt                            | Dixon (1997) AmMin 82(3-4):368-378, eq. 8                                                   |
| `Basalt_Lesne11`                | Generic alkali basalt                                | Lesne et al. (2011) CMP 162:153-168, eqs. 25-26                                             |
| `VesuviusAlkaliBasalt_Lesne11`  | Vesuvius alkali basalt (VES-9)                       | Lesne et al. (2011) CMP 162:153-168, Table 4                                                |
| `EtnaAlkaliBasalt_Lesne11`      | Etna alkali basalt (ETN-1)                           | Lesne et al. (2011) CMP 162:153-168, Table 4                                                |
| `StromboliAlkaliBasalt_Lense11` | Stromboli alkali basalt (PST-9)                      | Lesne et al. (2011) CMP 162:153-168, Table 4 (note typo `Lense11` in the upstream string) |
| `Basanite_Holloway94`           | Basanite / Si-undersaturated alkali basalt           | Holloway & Blank (1994) RiMG 30:187-230, Table 5                                            |
| `Leucitite_Thibault94`          | Leucitite / strongly Si-undersaturated alkali magmas | Thibault & Holloway (1994) CMP 116:216-224                                                  |
| `Rhyolite_Blank93`              | Rhyolite                                             | Blank et al. (1993) EPSL 119:27-36, Fig. 2 caption                                          |

For a worked example that uses three different `co2_sol` values across four basalts (Dixon for MORB and Kilauea, Allison-style for Fuego, Thibault for Fogo), see the `volfe.overrides` block in the Sulfur Comparison Paper config. Several upstream Allison-2019 entries (Sunset Crater, Erebus, Vesuvius, Etna, Stromboli) are commented out in VolFe's `C_CO3` function and not active — they are documented here only because the strings appear in the upstream source.

### `volfe.fo2_column` and `volfe.fo2_source`

How VolFe's initial fO2 is set at the top of the degassing run. Default `fo2_column = "Fe3FeT"`, `fo2_source = "auto"`. Both keys are volcatenate wrapper settings; VolFe itself has no native equivalent — VolFe accepts whichever redox column you place in `setup_df`, and the wrapper picks which one to put there.

#### VolFe's internal representation of fO2

VolFe stores the redox state internally as `melt_wf["Fe3FeT"]` — a melt Fe³⁺/FeT mole ratio, not a logarithm of fO2. Whatever sample column is supplied, VolFe converts it to that internal Fe3+/FeT via `mg.Fe3FeT_i` in `VolFe/melt_gas.py`, which dispatches on which redox indicator is present:

| Source of redox value (column in `setup_df`) | YAML key choosing the conversion equation    | Conversion to internal `Fe3FeT_i`                                                                                                                                                              |
| ---------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `Fe3FeT` (mole ratio on the sample)          | (none — direct assignment)                  | `Fe3FeT_i = melt_wf["Fe3FeT_i"]`. No conversion.                                                                                                                                               |
| `DNNO`                                       | `volfe.nno_buffer` and `volfe.fo2_model` | NNO buffer expression converts `dNNO` → log10(fO2) → fO2 (via `Frost91` in `mdv.NNO`); then `mdv.fO22Fe3FeT` converts fO2 → Fe3FeT_i using `volfe.fo2_model` (Kress91A by default). |
| `DFMQ`                                       | `volfe.fmq_buffer` and `volfe.fo2_model` | FMQ buffer expression (`Frost91` or `ONeill87` in `mdv.FMQ`) converts `dFMQ` → log10(fO2) → fO2; then `mdv.fO22Fe3FeT` converts fO2 → Fe3FeT_i using `volfe.fo2_model`.           |

P (bar) is the current pressure; T (Kelvin) is the run temperature. Buffer expressions and Fe3FeT ↔ fO2 conversions are P- and T-dependent, so the same `dFMQ` value at different P / T yields different absolute fO2 and a different Fe3FeT_i.

Volcatenate's standardized output reports `logfO2` (= log10 of absolute fO2), `dFMQ`, and `Fe3Fet_m` columns; all three are derived from VolFe's `Fe3FeT_i` plus the chosen `fo2_model` / `fmq_buffer` / `nno_buffer`.

Once `Fe3FeT_i` is set at the saturation pressure, the redox state evolves with pressure during degassing if `volfe.eq_fe = "yes"` (default). With `volfe.eq_fe = "no"`, Fe is frozen (`wt_Fe = 0` is set internally) and the gas-phase chemistry decouples from iron entirely.

#### Disambiguation: `fo2_column` vs. `fo2_source` vs. `fo2_model`

These are three independent settings the user controls separately:

- `volfe.fo2_column` selects the **preferred input column** — which sample field the wrapper writes into VolFe's `setup_df` (`Fe3FeT`, `DNNO`, or `DFMQ`).
- `volfe.fo2_source` selects the **strictness** of that choice — `"auto"` allows fallback to other sample fields if the preferred one is missing; `"fe3fet"`, `"dnno"`, `"dfmq"` require that exact column on the sample and raise if it is missing.
- `volfe.fo2_model` selects the **conversion equation** VolFe uses inside the Fe³⁺/FeT ↔ fO2 mapping. It is consulted whenever VolFe needs to translate between Fe3+/FeT and fO2 — i.e., during initialization on any path that goes through fO2 (`DNNO` and `DFMQ` paths), and at every pressure step when `eq_fe = "yes"` (because Fe3+/FeT and fO2 are interconverted as the run evolves), regardless of which path was used to initialize.

Setting `fo2_column: "DFMQ"` and `fo2_model: "Borisov18"` is meaningful: the dFMQ path is used for initialization (`Frost91` FMQ → fO2, then Borisov18 fO2 → Fe3FeT_i), and Borisov18 is then used at every subsequent step to keep Fe3+/FeT and fO2 in sync.

#### Per-mode behavior of `fo2_source`

| `fo2_source` value | Sample field used (set in CSV / dict /`MeltComposition`)                        | Initial `Fe3FeT_i` from                                | Behavior when the required sample field is missing                                             |
| -------------------- | --------------------------------------------------------------------------------- | -------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `auto` (default)   | `fo2_column` first, then falls back through `Fe3FeT` → `dNNO` → `dFMQ`  | the equation matching the chosen field (see table above) | Logs each fallback at INFO. Raises `ValueError` only if none of the three are on the sample. |
| `fe3fet`           | `Fe3FeT` (or speciated `FeO + Fe2O3`, computed into `comp.fe3fet_computed`) | direct (`Fe3FeT_i = sample value`)                     | Raises `ValueError`.                                                                         |
| `dnno`             | `dNNO`                                                                          | NNO buffer → fO2 → Fe3FeT_i (uses `fo2_model`)       | Raises `ValueError`.                                                                         |
| `dfmq`             | `dFMQ`                                                                          | FMQ buffer → fO2 → Fe3FeT_i (uses `fo2_model`)       | Raises `ValueError`.                                                                         |

Multiple redox indicators on the sample do not conflict at the volcatenate boundary — the wrapper sends only one column to VolFe. Upstream VolFe accepts more redox columns than the wrapper exposes (`logfO2_i`, speciated `FeO + Fe2O3`, `S6ST_i`); when more than one is set on a `setup_df` row, VolFe's own internal precedence is `Fe3FeT_i` → `logfO2_i` → `DNNO` → `DFMQ` → `S6ST_i` → `FeO + Fe2O3`, with a warning emitted for each additional column. Volcatenate preempts that path by writing exactly one redox column, so users do not see those upstream warnings.

#### Output columns

The choice of `fo2_column` / `fo2_source` directly determines the initial values of the `logfO2`, `dFMQ`, and `Fe3Fet_m` columns in the standardized output CSV. The chosen initial fO2 then propagates through S speciation (`SCSS` / `SCAS` / `S6/ST`) and vapor composition for the rest of the run.

#### Initialization vs. evolution

`fo2_column` / `fo2_source` only fix the starting state at the top of the degassing path. Whether fO2 evolves with pressure during degassing is gated by `volfe.eq_fe` (default `"yes"`, evolves).

#### Which other VolFe YAML keys interact with these

Several VolFe keys are read or ignored conditional on the chosen redox column (`volfe.fo2_model`, `volfe.fmq_buffer`, `volfe.nno_buffer`, `volfe.eq_fe`). For the complete map of which keys are read in which mode and which are ignored, see [fO2-related YAML keys: precedence and which keys are ignored when others are set](#fo2-related-yaml-keys-precedence-and-which-keys-are-ignored-when-others-are-set) in the universal Redox section above.

### Wrapper-managed values not in the YAML — VolFe

These are sent to VolFe by volcatenate but are not in the YAML. They are either hardcoded in `_build_models_df` in [backends/volfe.py](../src/volcatenate/backends/volfe.py), pulled directly from the `MeltComposition`, or left to VolFe's built-in defaults via `vf.make_df_and_add_model_defaults`.

| VolFe option / setup_df column                                                                 | volcatenate value                                     | Meaning                                                                                                                                                                                                                                               |
| ---------------------------------------------------------------------------------------------- | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `output csv`                                                                                 | `"False"`                                           | Volcatenate handles output itself; tells VolFe not to write its own CSVs.                                                                                                                                                                             |
| `print status`                                                                               | `"False"`                                           | Tells VolFe not to print progress; the wrapper captures stdout via `_quiet_volfe()` and routes it through the volcatenate logger.                                                                                                                   |
| `solve_species`                                                                              | (left to default `"auto"`)                          | Internal solver hint. VolFe re-sets it during the calculation in `solve_species` in `VolFe/equilibrium_equations.py`, so any user-set value would be clobbered. Volcatenate keeps it out of the YAML to make this clear.                          |
| `mass_volume`                                                                                | (left to default `"mass"`)                          | The `"volume"` branch is marked NEEDS FIXING upstream and is unsafe to expose; volcatenate relies on VolFe's `"mass"` default.                                                                                                                    |
| `setup`                                                                                      | (left to default `"False"`)                         | Debug-only flag; not exposed.                                                                                                                                                                                                                         |
| `Sample`, `T_C`, all major oxides (incl. `FeOT`)                                         | from `MeltComposition`                              | Read directly off the user's sample.                                                                                                                                                                                                                  |
| `H2O` (wt%), `CO2ppm` (= `CO2_wt%` × 10000), `STppm` (= `S_wt%` × 10000), `Xppm` | from `MeltComposition`                              | Read directly off the user's sample. The wt% → ppm conversions for CO2 and S happen in `_build_setup_df`.                                                                                                                                          |
| `Fe3FeT` / `DNNO` / `DFMQ` (one of)                                                      | from `MeltComposition` via `_resolve_volfe_redox` | Exactly one redox column, picked by `volfe.fo2_column` and `volfe.fo2_source` per the deep-dive subsection above. Upstream VolFe's full set of redox columns (`logfO2_i`, `S6ST_i`, speciated `FeO + Fe2O3`) is not exposed by volcatenate. |

---

## EVo

The options set by the user in volcatenate's configuration YAML file and in user sample inputs (CSV, `MeltComposition` object, or Python dict) are used to populate the three YAML files (`chem.yaml`, `env.yaml`, `output.yaml`) that EVo reads. Volcatenate writes all three to a per-sample work directory, then calls `evo.run_evo()`. EVo prints prolifically to stdout; volcatenate wraps the call in a `_quiet_evo()` context that routes the captured output to the volcatenate logger at DEBUG level (which lands in `RunConfig.log_file` if you set one).

### Files saved per EVo run

For one EVo run on a sample called `Fuego`, the on-disk layout produced is:

```
<output_dir>/
├── EVo/
│   └── fuego.csv                              # standardized degassing path (always)
├── saturation_pressures.csv                   # row for each (sample, model) (when running calculate_saturation_pressure)
├── resolved_inputs/
│   └── Fuego/
│       └── EVo.yaml                           # exact env+chem+output dicts the wrapper handed EVo (when save_bundle is on)
├── <save_bundle>.json                         # full run bundle including resolved_inputs (when save_bundle is set)
└── <raw_output_dir>/                          # = "raw_tool_output" by default
    └── Fuego_evo_degas/                       # (or _evo_satp for sat-pressure calls)
        ├── chem.yaml                          # only when keep_raw_output: true
        ├── env.yaml                           # only when keep_raw_output: true
        ├── output.yaml                        # only when keep_raw_output: true
        └── output/
            └── dgs_output_*.csv               # EVo's own native degassing output (only when keep_raw_output: true)
```

When `keep_raw_output: false`, the entire `<raw_output_dir>/Fuego_evo_degas/` subtree is deleted at the end of the run. The standardized degassing CSV (`EVo/fuego.csv`), the `resolved_inputs/Fuego/EVo.yaml` sidecar, and the run bundle JSON are unaffected by `keep_raw_output` — they always survive.

The same on-disk layout is documented in the [Top-level RunConfig settings](#on-disk-layout-the-top-level-fields-produce) section above; the file list here is the EVo-specific subset.

### How the YAML propagates

```
volcatenate_config.yaml
  └── evo:
       └── RunConfig.evo (EVoConfig dataclass)
            └── _write_yaml_configs()
                 ├── chem.yaml   (oxides + Fe split)
                 ├── env.yaml    (run options + volatiles + fO2)
                 └── output.yaml (plot toggles, all False)
                      └── evo.run_evo(chem, env, output)
```

Wrapper code: [backends/evo.py](../src/volcatenate/backends/evo.py).

### Field-by-field — EVo

The "env.yaml key" column gives the exact key written into EVo's `env.yaml`, `chem.yaml`, or `output.yaml` — the files EVo actually reads. Defaults are explicit. Allowed values are enumerated when finite; for continuous fields the column gives a Python type plus a bound. Where a string field accepts case-insensitive forms, that note is in the Behavior column.

| volcatenate YAML key                                   | Default                                       | Allowed values (with type)                                                                                                                                                                                                                                                | Behavior                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | env.yaml key                                            |
| ------------------------------------------------------ | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| `gas_system`                                         | `cohs`                                      | `str`: `OH`, `COH`, `SOH`, `COHS`, `COHSN`                                                                                                                                                                                                                    | Selects which elements the gas phase tracks. The letters name the elements:`OH` = H, O only; `COH` = C, H, O; `SOH` = S, H, O; `COHS` = C, O, H, S (the standard sulfur-bearing system, volcatenate's default); `COHSN` = COHS plus N. Case-insensitive.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `GAS_SYS`                                             |
| `composition`                                        | `basalt`                                    | `str`: `basalt`, `phonolite`, `rhyolite`                                                                                                                                                                                                                          | **This setting does not set the actual chemical composition of the melt** (which always comes from the sample's major-oxide fields). Selects which table of solubility-law coefficients EVo's `Molecule.__init__` uses for each species. EVo stores per-species `a` and `b` solubility constants in `cnst.basalt`, `cnst.phonolite`, and `cnst.rhyolite` lookup tables — read inside `Molecule.__init__` in `EVo/dgs_classes.py`; `composition` chooses which table is read. For phonolite and rhyolite, H2O (and CO2 for rhyolite) additionally use temperature-dependent expressions within tested T ranges (`cnst.phonolite["a"][Mol](T)`); outside those ranges, fixed `a, b` constants are substituted. EVo also runs a SiO2 sanity check (`run_melt_match` in `EVo/readin.py`): basalt expects 45–55 wt%, phonolite 52–63, rhyolite 65–80. If the sample's SiO2 is out of range EVo would normally prompt for confirmation; volcatenate auto-confirms via `_auto_yes`. | `COMPOSITION`                                         |
| `fo2_buffer`                                         | `FMQ`                                       | `str`: `FMQ`, `NNO`, `IW`                                                                                                                                                                                                                                         | Names the redox buffer EVo uses when fO2 is set as a buffer-relative offset. Read by EVo only when (a) you set `evo.fo2_source: buffer`, or (b) `evo.fo2_source: auto` *and* the sample carries none of `Fe3FeT`, `dNNO`, or `dFMQ` — in that last-resort case the wrapper writes the named buffer to `env.yaml` with offset 0 and emits a `WARNING`. Otherwise written to `env.yaml` but ignored by EVo. See [fO2-related YAML keys: precedence](#fo2-related-yaml-keys-precedence-and-which-keys-are-ignored-when-others-are-set).                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `FO2_buffer`                                          |
| `fo2_source`                                         | `auto`                                      | `str`: `auto`, `fe3fet`, `buffer`, `absolute`                                                                                                                                                                                                                   | Volcatenate wrapper setting (no native EVo equivalent). Names the input pathway used to set the initial fO2. See the expanded `evo.fo2_source` subsection below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | (drives `FO2_buffer_SET`, `FO2_SET`, `FO2_START`) |
| `fe_system`                                          | `true`                                      | `bool`: `true`, `false`                                                                                                                                                                                                                                             | When `true` (default), EVo iterates Fe redox equilibrium with fO2 at every pressure step — fO2 evolves as Fe3+/FeT shifts during degassing. When `false`, Fe (and therefore the fO2 implied by the iron redox state) is held fixed at its initial value for the whole run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `FE_SYSTEM`                                           |
| `find_saturation`                                    | `true`                                      | `bool`: `true`, `false`                                                                                                                                                                                                                                             | Controls EVo's starting pressure. When `true` (default), EVo searches for the saturation pressure of the sample's bulk volatiles itself, then begins degassing at that pressure; `p_start` is read only as an upper-bound for the search. When `false`, EVo begins at exactly `p_start` and assumes that pressure is appropriate for the sample (no saturation-pressure search).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `FIND_SATURATION`                                     |
| `single_step`                                        | `false`                                     | `bool`: `true`, `false`                                                                                                                                                                                                                                             | When `true`, EVo runs a single equilibrium calculation at one pressure rather than a full degassing path. Has effect only when `find_saturation=false`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `SINGLE_STEP`                                         |
| `s_sat_warn`                                         | `false`                                     | `bool`: `true`, `false`                                                                                                                                                                                                                                             | Despite the name, this flag controls more than a warning. When `true` (and `gas_system` is `SOH` / `COHS` / `COHSN`), EVo computes SCSS at every pressure step and **terminates the run** if `S > SCSS` or if `S⁶⁺ / (S²⁻ + S⁶⁺) ≥ 0.1` — the standardized output stops at the saturation pressure rather than continuing with a sulfide phase. The warning is what gets routed through the volcatenate logger when termination fires; the run-termination behavior is upstream and is gated by the same flag. When `false` (volcatenate's default), EVo neither checks nor terminates and runs past saturation. See "Saturation enforcement" in the [Sulfur deep-dive](#sulfur-deep-dive-cross-backend-model-choices) for cross-backend context.                                                                                                                                                                                                                                        | `S_SAT_WARN`                                          |
| `atomic_mass_set`                                    | `false`                                     | `bool`: `true`, `false`                                                                                                                                                                                                                                             | Volatile-input mode switch.`false` (default) feeds the sample's `H2O`/`CO2`/`S` (wt%, from the `MeltComposition`) directly into EVo's chem.yaml. `true` ignores those sample values and uses `atomic_h`/`atomic_c`/`atomic_s`/`atomic_n` (ppm, from the YAML) instead. Volcatenate users typically leave this `false`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `ATOMIC_MASS_SET`                                     |
| `ocs`                                                | `false`                                     | `bool`: `true`, `false`                                                                                                                                                                                                                                             | When `true`, EVo includes carbonyl sulfide (OCS) as a vapor species, adding a coupling between C and S. The contribution is small except at low fO2 (≲ FMQ−2).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `OCS`                                                 |
| `dp_min`                                             | `1` (bar)                                   | `int`, `≥ 1`                                                                                                                                                                                                                                                         | Smallest pressure step the adaptive integrator is allowed to take.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `DP_MIN`                                              |
| `dp_max`                                             | `100` (bar)                                 | `int`, `≥ dp_min`                                                                                                                                                                                                                                                    | Largest pressure step. Decreasing `dp_max` makes the degassing curves smoother but slows the run linearly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `DP_MAX`                                              |
| `mass`                                               | `100` (g)                                   | `int` or `float`, `> 0`                                                                                                                                                                                                                                             | Total system mass for mass-balance bookkeeping. Intensive results (compositions, fO2, mole fractions) are independent of `mass`; only the absolute mass of vapor produced scales with it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `MASS`                                                |
| `p_start`                                            | `3000` (bar)                                | `int` or `float`, `> p_stop`                                                                                                                                                                                                                                        | Starting pressure (bar). When `find_saturation=true`, EVo treats `p_start` as the **search ceiling** for its saturation-pressure search; the run actually begins at the saturation pressure EVo finds (which is below `p_start` by definition of saturation). When `find_saturation=false`, the run begins at exactly `p_start`. Set `p_start` well above the expected saturation pressure for volatile-rich samples.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `P_START`                                             |
| `p_stop`                                             | `1` (bar)                                   | `int` or `float`, `> 0`, `< p_start`                                                                                                                                                                                                                              | Final pressure (bar) at the bottom of the degassing path.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `P_STOP`                                              |
| `wgt`                                                | `1e-5`                                      | `float`, `> 0`                                                                                                                                                                                                                                                        | Initial mass fraction of vapor seeded into the system to start the iteration. EVo's solver requires a non-zero gas mass to converge from;`1e-5` is the upstream-recommended value. Practical range is roughly `1e-7` to `1e-3`; values outside that range can cause the solver to fail or bias the initial vapor speciation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `WgT`                                                 |
| `loss_frac`                                          | `0.9999`                                    | `float`, `0 ≤ value ≤ 1`                                                                                                                                                                                                                                            | Per-step vapor*retention* fraction in open-system runs (`run_type=open`). At each pressure step, this fraction of the vapor is kept in equilibrium with the melt for the next step; the rest is removed from the system. `0.9999` keeps almost all vapor (effectively closed-system); `0.0` removes all vapor at each step (perfect open-system extraction). The parameter is named for vapor *loss* but parameterized as the fraction *retained*.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `LOSS_FRAC`                                           |
| `run_type`                                           | `closed`                                    | `str`: `closed`, `open`                                                                                                                                                                                                                                             | Closed- vs. open-system degassing. Open-system runs require `loss_frac < 1`. The wrapper hardcodes `closed` for saturation-pressure-only runs (regardless of `run_type` in the config) — see [backends/evo.py:132](../src/volcatenate/backends/evo.py).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `RUN_TYPE`                                            |
| `atomic_h`, `atomic_c`, `atomic_s`, `atomic_n` | `500`, `200`, `4000`, `10` (ppm each) | `float`, `≥ 0`                                                                                                                                                                                                                                                       | Per-element bulk volatile budgets in ppm. Used only when `atomic_mass_set=true`; ignored otherwise.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `ATOMIC_H`, `ATOMIC_C`, `ATOMIC_S`, `ATOMIC_N`  |
| `nitrogen_set`                                       | `false`                                     | `bool`: `true`, `false`                                                                                                                                                                                                                                             | When `true`, EVo tracks nitrogen as a system component. When `false` (default), N is excluded entirely.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `NITROGEN_SET`                                        |
| `nitrogen_start`                                     | `0.0001` (mass fraction)                    | `float`, `> 0`                                                                                                                                                                                                                                                        | Starting nitrogen mass fraction. Readonly when `nitrogen_set=true`, and then only as a fallback: if the sample carries `N_ppm` (via the `Nitrogen` / `Nppm` / `N_ppm` CSV column or `MeltComposition.N_ppm`), the wrapper uses that value (converted ppm → mass fraction) instead and `nitrogen_start` is ignored. See [`MeltComposition` field reference](sample_data.md).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `NITROGEN_START`                                      |
| `graphite_saturated`                                 | `false`                                     | `bool`: `true`, `false`                                                                                                                                                                                                                                             | When `true`, EVo starts the system as graphite-saturated (paired with `graphite_start` for the initial graphite mass fraction). Most natural basalts are not graphite-saturated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `GRAPHITE_SATURATED`                                  |
| `graphite_start`                                     | `0.0001` (mass fraction)                    | `float`, `> 0`                                                                                                                                                                                                                                                        | Starting graphite mass fraction. Read only when `graphite_saturated=true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `GRAPHITE_START`                                      |
| `fo2_set`                                            | `false`                                     | `bool`: `true`, `false`                                                                                                                                                                                                                                             | Switch for absolute-fO2 input. Read only when `fo2_source: absolute`; in that mode `fo2_set` must be `true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `FO2_SET`                                             |
| `fo2_start`                                          | `0.0` (bar)                                 | `float`, `≥ 0`                                                                                                                                                                                                                                                       | Absolute fO2 value (bar). Read only when `fo2_source: absolute` and `fo2_set=true`; in that mode must be `> 0`. Otherwise written to `env.yaml` but ignored by EVo.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `FO2_START`                                           |
| `fh2_set`                                            | `false`                                     | `bool`: `true`, `false`                                                                                                                                                                                                                                             | Switch to set H2 fugacity as an explicit starting condition. Read only when `true`; otherwise EVo derives H2 from H2O + fO2 via the H2 solubility model.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `FH2_SET`                                             |
| `fh2_start`                                          | `0.24` (bar)                                | `float`, `> 0`                                                                                                                                                                                                                                                        | H2 fugacity (bar) used when `fh2_set=true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `FH2_START`                                           |
| `fh2o_set`                                           | `false`                                     | `bool`: `true`, `false`                                                                                                                                                                                                                                             | Switch to set H2O fugacity as an explicit starting condition. Read only when `true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `FH2O_SET`                                            |
| `fh2o_start`                                         | `1000.0` (bar)                              | `float`, `> 0`                                                                                                                                                                                                                                                        | H2O fugacity (bar) used when `fh2o_set=true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `FH2O_START`                                          |
| `fco2_set`                                           | `false`                                     | `bool`: `true`, `false`                                                                                                                                                                                                                                             | Switch to set CO2 fugacity as an explicit starting condition. Read only when `true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `FCO2_SET`                                            |
| `fco2_start`                                         | `1.0` (bar)                                 | `float`, `> 0`                                                                                                                                                                                                                                                        | CO2 fugacity (bar) used when `fco2_set=true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `FCO2_START`                                          |
| `h2o_model`                                          | `burguisser2015`                            | `str`: `burguisser2015` (Burguisser, A., Alletti, M., Scaillet, B. (2015) Computers & Geosciences 79:1–14) (only option recognized by upstream EVo at present)                                                                                                       | Which H2O solubility law EVo uses to relate dissolved melt-H2O to H2O fugacity.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `H2O_MODEL`                                           |
| `h2_model`                                           | `gaillard2003`                              | `str`: `gaillard2003` (Gaillard, F., Schmidt, B., Mackwell, S., McCammon, C. (2003) GCA 67:2427–2441), `burguisser2015` (Burguisser, A., Alletti, M., Scaillet, B. (2015) Computers & Geosciences 79:1–14)                                                        | H2 solubility law. Has effect only at reduced fO2 where dissolved H2 is non-trivial.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `H2_MODEL`                                            |
| `c_model`                                            | `burguisser2015`                            | `str`: `burguisser2015` (Burguisser, A., Alletti, M., Scaillet, B. (2015) Computers & Geosciences 79:1–14), `eguchi2018` (Eguchi, J., Dasgupta, R. (2018) Chemical Geology 487:23–38 — CO2 solubility model for silicate melts as a function of oxygen fugacity) | CO2 / CO3²⁻ solubility law. Pair `c_model` with the H2O law from the same source paper for internal consistency. `eguchi2018` exposes additional graphite-saturation handling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `C_MODEL`                                             |
| `co_model`                                           | `armstrong2015`                             | `str`: `armstrong2015` (Armstrong, L.S., Hirschmann, M.M., Stanley, B.D., Falksen, E.G., Jacobsen, S.D. (2015) GCA 171:283–302 — speciation and solubility of reduced C-O-H-N volatiles in mafic melts), `None` (literal string `None`, disables CO solubility) | CO solubility law. Has effect only at reduced fO2 where dissolved CO is non-trivial.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `CO_MODEL`                                            |
| `ch4_model`                                          | `ardia2013`                                 | `str`: `ardia2013` (Ardia, P., Hirschmann, M.M., Withers, A.C., Stanley, B.D. (2013) GCA 114:52–71 — solubility of CH4 in synthetic basaltic melt), `None` (disables CH4 solubility)                                                                              | CH4 solubility law. Has effect only at strongly reduced fO2 (≲ FMQ−2).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `CH4_MODEL`                                           |
| `sulfide_capacity`                                   | `oneill2020`                                | `str`: `oneill2020`, `oneill2002` (O'Neill, H.S.C., Mavrogenes, J.A. (2002) Journal of Petrology 43:1049–1087 — sulfide capacity of silicate melts)                                                                                                               | Sulfide capacity model — controls how much S²⁻ the melt holds at given fO2 and composition. Affects S degassing under reducing conditions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | `SULFIDE_CAPACITY`                                    |
| `sulfate_capacity`                                   | `nash2019`                                  | `str`: `nash2019` (Nash, W.M., Smythe, D.J., Wood, B.J. (2019) EPSL 507:187–198 — compositional and temperature effects on sulfur speciation) (only option recognized at present)                                                                                   | Sulfate capacity model. Affects S degassing under oxidizing conditions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `SULFATE_CAPACITY`                                    |
| `scss`                                               | `liu2007`                                   | `str`: `liu2007` (Liu, Y., Samaha, N-T., Baker, D.R. (2007) GCA 71:1783–1799 — sulfur concentration at sulfide saturation) (only option recognized at present)                                                                                                      | Sulfide content at sulfide saturation (SCSS) model. Read by EVo when sulfide saturation is reached during the run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `SCSS`                                                |
| `n_model`                                            | `libourel2003`                              | `str`: `libourel2003` (Libourel, G., Marty, B., Humbert, F. (2003) GCA 67:4123–4135 — nitrogen solubility in basaltic melt) (only option recognized at present)                                                                                                     | Nitrogen solubility law. Read only when `nitrogen_set=true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | `N_MODEL`                                             |
| `density_model`                                      | `spera2000`                                 | `str`: `spera2000` (Spera, F.J. (2000), 'Physical properties of magma' in Encyclopedia of Volcanoes (Sigurdsson et al., eds.)) (only option recognized at present)                                                                                                    | Melt density model used for mass ↔ volume conversion in the bookkeeping. Does not affect intensive quantities like fO2 or species mole fractions.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `DENSITY_MODEL`                                       |
| `fo2_model`                                          | `kc1991`                                    | `str`: `kc1991` (Kress & Carmichael (1991) CMP 108:82–92, appendix eq. A-5), `r2013` (Righter (2013) — Fe redox model calibrated for FeOt > 15 wt% melts)                                                                                                         | Fe3+/FeT ↔ logfO2 conversion equation EVo uses internally.`kc1991` = Kress & Carmichael (1991) appendix eq. A-5; `r2013` = Righter (2013). Read whenever EVo needs to translate between Fe3+/FeT and fO2 — i.e., during initialization on the Fe3+/FeT path, and at every pressure step when `fe_system=true`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | `FO2_MODEL`                                           |
| `fmq_model`                                          | `frost1991`                                 | `str`: `frost1991` (Frost (1991) Reviews in Mineralogy & Geochemistry vol. 25, ch. 1) (only option recognized at present)                                                                                                                                             | Definition of the FMQ buffer (and, in volcatenate's wrapper, also of the NNO and IW buffers via Frost 1991 expressions). Read by EVo (a) during initialization on the buffer path, when converting `dFMQ`/`dNNO` from the sample to absolute fO2; (b) at every pressure step when reporting `dFMQ`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `FMQ_MODEL`                                           |
| `overrides`                                          | `{}`                                        | `dict[str, dict[str, Any]]`. Example: `{"MORB": {"dp_max": 25}, "Fogo": {"p_start": 5000, "gas_system": "coh"}}`                                                                                                                                                      | Per-sample overrides keyed by sample name; values override the corresponding fields above for that sample only. Nested-dataclass fields can be overridden as partial dicts (the wrapper merges them onto the parent). See[Per-sample overrides in configuration.md](configuration.md#per-sample-overrides).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | (resolved in `resolve_sample_config`)                 |

### `evo.fo2_source`

How EVo's initial fO2 is set at the top of the degassing run. Default `auto`. Allowed values: `auto`, `fe3fet`, `buffer`, `absolute`.

#### EVo's internal representation of fO2

EVo represents fO2 internally as `sys.FO2 = ln(absolute fO2 in bar)` — natural log, not log10. EVo also tracks `sys.FO2_buffer` (the offset relative to a chosen reference buffer — FMQ, NNO, or IW) for reporting. Volcatenate's standardized output reports `logfO2` (= log10 of absolute fO2) and `dFMQ` columns; both are derived from those internal quantities.

Whatever sample field you use to initialize the run, EVo converts it to that internal `sys.FO2` (= ln fO2 in bar) via one of three equations. The equation used in each path is itself selected by other YAML keys (`evo.fo2_model`, `evo.fmq_model`); the user controls both the input pathway (via `fo2_source`) and the conversion equation (via `fo2_model` / `fmq_model`):

| Source of redox value                                                                                           | YAML key choosing the conversion equation | Conversion to internal `sys.FO2`                                                                                                                                                                                                                 |
| --------------------------------------------------------------------------------------------------------------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Fe3FeT` (or speciated `FeO + Fe2O3`) on the sample                                                         | `evo.fo2_model`                         | Translates Fe3+/FeT (with melt composition, T in K, P in bar) → log10(fO2) → ln(fO2) via the chosen equation.`<br>` `kc1991` (default): Kress & Carmichael (1991) appendix eq. A-5. `<br>` `r2013`: Righter (2013).                      |
| `dFMQ` on the sample                                                                                          | `evo.fmq_model`                         | Translates `dFMQ` (with T in K, P in bar) → log10(fO2) → ln(fO2). `<br>` `frost1991` (default; only option in upstream EVo): Frost (1991) FMQ parameterization.                                                                            |
| `dNNO` on the sample                                                                                          | `evo.fmq_model`                         | Translates `dNNO` (with T in K, P in bar) → log10(fO2) → ln(fO2). `<br>` `frost1991` (default; only option): Frost (1991) NNO parameterization. (Same `fmq_model` setting controls both buffer families because Frost 1991 covers both.) |
| `evo.fo2_start` (YAML; absolute fO2 in bar; only when `evo.fo2_source: absolute` and `evo.fo2_set: true`) | (none — direct assignment)               | `sys.FO2 = ln(fo2_start)`. EVo also back-computes `sys.FO2_buffer = log10(fO2) − log10(fO2_FMQ)(T,P)` for reporting.                                                                                                                          |

T (Kelvin) is the run temperature; P (bar) is the current pressure. All four conversions are P- and T-dependent, so the same input value at different P/T yields different absolute fO2.

Once `sys.FO2` is set at the saturation pressure, fO2 evolves with pressure during degassing if `evo.fe_system=true` (default). With `evo.fe_system=false`, `sys.FO2` is held at its initial value for the whole run.

#### Disambiguation: `fo2_source` vs. `fo2_model`

These are two independent settings the user controls separately:

- `evo.fo2_source` selects the **input pathway** — which sample field volcatenate reads to derive the initial fO2 (`Fe3FeT`, `dNNO`, `dFMQ`, or none if `absolute`).
- `evo.fo2_model` selects the **conversion equation** EVo uses inside the Fe3+/FeT pathway to map `Fe3+/FeT ↔ logfO2`. It is also consulted at every pressure step when `fe_system=true` (because Fe3+/FeT and fO2 are interconverted as the run evolves), regardless of which path was used to initialize.

Setting `evo.fo2_source: buffer` and `evo.fo2_model: r2013` is meaningful: the buffer path is used for initialization, and Righter 2013 is used at every subsequent step to keep Fe3+/FeT and fO2 in sync.

#### Per-mode behavior of `fo2_source`

| `fo2_source` value | Sample field used (set in CSV / dict /`MeltComposition`)                                                                                                                              | Initial `sys.FO2` from                                 | Behavior when the required sample field is missing                                                                                                                                                                                           |
| -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `auto` (default)   | best available, in priority order:`Fe3FeT` → `dNNO` → `dFMQ`                                                                                                                    | the equation matching the chosen field (see table above) | Falls back to `evo.fo2_buffer` (from the YAML) at offset 0; emits a `WARNING`-level log entry. Never raises.                                                                                                                             |
| `fe3fet`           | `Fe3FeT` (or speciated `FeO + Fe2O3`)                                                                                                                                               | KC91 / Righter via `evo.fo2_model`                     | Raises `ValueError`.                                                                                                                                                                                                                       |
| `buffer`           | `dNNO` if `evo.fo2_buffer: NNO`; `dFMQ` if `evo.fo2_buffer: FMQ`. (`IW` is allowed as a buffer choice but uses offset 0 since `comp.dIW` does not exist as a sample field.) | Frost-1991 buffer expression via `evo.fmq_model`       | Raises `ValueError` if `evo.fo2_buffer` is `FMQ` or `NNO` and the matching offset is missing.                                                                                                                                        |
| `absolute`         | (none — uses the YAML's `evo.fo2_start`)                                                                                                                                             | direct:`sys.FO2 = ln(fo2_start)`                       | Raises `ValueError` if `evo.fo2_set` is not `true` or `evo.fo2_start ≤ 0`. Side effect: `chem.yaml` does not split `FeOT` into `FeO + Fe2O3` because EVo refuses to run with both an absolute fO2 and a speciated iron entry. |

`auto` mode logs each path choice at INFO level, except the `evo.fo2_buffer`-with-offset-0 last fallback, which is logged at WARNING so it is visible at any log level. Multiple redox indicators on the sample do not conflict — `Fe3FeT` wins.

#### Output columns

The choice of `fo2_source` directly determines the initial values of the `logfO2`, `dFMQ`, and `Fe3Fet_m` columns in the standardized output CSV. The chosen initial fO2 then propagates through S speciation and vapor composition for the rest of the run.

#### Initialization vs. evolution

`fo2_source` only fixes the starting state at the top of the degassing path. Whether fO2 evolves with pressure during degassing is gated by `evo.fe_system` (default `true`, evolves).

#### Which other EVo YAML keys interact with this one

`evo.fo2_source` is the dispatch switch for redox initialization, and several other EVo keys are read or ignored conditional on its value (`evo.fo2_buffer`, `evo.fo2_set`/`fo2_start`, `evo.fo2_model`, `evo.fmq_model`, the `evo.fh*_set` fields). For the complete map of which keys are read in which mode and which are ignored, see [fO2-related YAML keys: precedence and which keys are ignored when others are set](#fo2-related-yaml-keys-precedence-and-which-keys-are-ignored-when-others-are-set) in the universal Redox section above.

### Wrapper-managed values not in the YAML — EVo

In `_write_yaml_configs` at [backends/evo.py](../src/volcatenate/backends/evo.py):

| env.yaml key                                                         | volcatenate value                                               | Meaning                                                                                                                                                |
| -------------------------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `RUN_TYPE`                                                         | `"closed"` for satP runs; `cfg.run_type` for degassing runs | Volcatenate forces `run_type="closed"` for the saturation-pressure call regardless of config, then honors `cfg.run_type` for the degassing run.    |
| `WTH2O_SET`, `WTCO2_SET`, `SULFUR_SET`                         | `True`                                                        | Always; values come from `comp.H2O / 100`, `comp.CO2 / 100`, `comp.S / 100` (wt% → weight fraction).                                            |
| `WTH2O_START`, `WTCO2_START`, `SULFUR_START`                   | computed from `MeltComposition`                               | See above.                                                                                                                                             |
| `T_START`                                                          | `comp.T_C + 273.15`                                           | Kelvin.                                                                                                                                                |
| `FO2_buffer_SET`, `FO2_SET`, `FO2_buffer_START`, `FO2_START` | computed from `cfg.fo2_source`                                | These four are populated together by `_resolve_fo2_source` based on `fo2_source` and the indicators on the sample. See the fallback section below. |

#### chem.yaml zero-oxide handling

When an oxide is exactly 0 in the sample, volcatenate writes `1e-10` to `chem.yaml` instead. EVo's internal `single_cat()` drops keys with value 0, which causes downstream functions to raise `KeyError` on species like `mno`. The 1e-10 substitute keeps the key present without affecting the chemistry. See [backends/evo.py:403](../src/volcatenate/backends/evo.py).

#### Iron split

In `chem.yaml`, FeOT is split into FeO + Fe2O3 using `comp.fe3fet_computed` ([backends/evo.py](../src/volcatenate/backends/evo.py)):

```
FEO   = FeOT * (1 - Fe3FeT)
FE2O3 = FeOT * Fe3FeT * (159.69 / (2 * 71.844))   # MW conversion
```

If `fe3fet_computed` is NaN (no redox indicator at all), the wrapper sends `FEO = FeOT, FE2O3 = 0`, leaving fO2 to be set by the buffer. If `fo2_source="absolute"`, the wrapper deliberately **skips** the split and sends `FEO = FeOT, FE2O3 = 0` even when Fe3+/FeT is available — EVo refuses to accept both `FO2_SET=True` and a non-zero Fe2O3 simultaneously (`readin_chem` in [EVo/readin.py]).

#### output.yaml

Always written with all five plot flags `False`. EVo's plotting is unused because volcatenate makes its own figures.

---

## MAGEC

MAGEC (Sun & Yao 2024) is a compiled MATLAB solver (a `.p` file), not a Python library. The user's volcatenate YAML and sample inputs are translated into a per-pressure-step input CSV and a MATLAB `struct(...)` literal of run settings. Volcatenate writes a small `.m` script that adds the bundled CSV wrapper and the `MAGEC_Solver_v1b.p` solver directory to the MATLAB path, then calls MATLAB once with `matlab -batch "eval(fileread(script.m))"`. The MATLAB-side wrapper converts CSV → xlsx, runs the `.p` solver, and converts the output xlsx → CSV; volcatenate reads the CSV back. MAGEC's MATLAB stdout is captured and routed to the volcatenate logger at DEBUG.

### Files saved per MAGEC run

For one MAGEC degassing run on a sample called `Fuego`, the on-disk layout produced is:

```
<output_dir>/
├── MAGEC/
│   └── fuego.csv                              # standardized degassing path (always)
├── saturation_pressures.csv                   # row for each (sample, model) (when running calculate_saturation_pressure)
├── resolved_inputs/
│   └── Fuego/
│       └── MAGEC.yaml                         # exact input first-row + settings struct (when save_bundle is on)
├── <save_bundle>.json                         # full run bundle including resolved_inputs (when save_bundle is set)
└── <raw_output_dir>/                          # = "raw_tool_output" by default
    └── magec/
        └── Fuego/                             # (for batch satP, this is `_batch_satp/`)
            ├── Fuego_input.csv                # only when keep_raw_output: true
            ├── Fuego_output.csv               # only when keep_raw_output: true
            └── _matlab_scripts/               # only when keep_raw_output: true (script file is removed at the end of each successful run)
```

When `keep_raw_output: false`, the entire `<raw_output_dir>/magec/Fuego/` subtree is deleted at the end of the run. The standardized degassing CSV (`MAGEC/fuego.csv`), the `resolved_inputs/Fuego/MAGEC.yaml` sidecar, and the run bundle JSON are unaffected by `keep_raw_output` — they always survive. The intermediate xlsx files that MAGEC's CSV wrapper produces inside MATLAB are written into the same per-sample directory and removed by the wrapper before the script returns.

The same on-disk layout is documented in the [Top-level RunConfig settings](#on-disk-layout-the-top-level-fields-produce) section above; the file list here is the MAGEC-specific subset.

### How the YAML propagates

```
volcatenate_config.yaml
  └── magec:
       └── RunConfig.magec (MAGECConfig dataclass)
            ├── _build_sample_input_rows()    → per-pressure-step rows for the input CSV
            └── _build_settings_matlab_struct() → MATLAB struct(...) literal
                 └── _run_magec_matlab()
                      └── matlab -batch "MAGEC_CSV_Wrapper(in.csv, out.csv, settings)"
                           └── MAGEC_Solver_v1b.p
```

Wrapper code: [backends/magec.py](../src/volcatenate/backends/magec.py).

### Field-by-field — MAGEC

The "MATLAB struct field" column gives the exact field name in the MATLAB `struct(...)` literal MAGEC consumes (or, for the redox / pressure / composition fields, the input-CSV column name MAGEC reads on each row). Defaults are explicit. Allowed values are enumerated when finite; for continuous fields the column gives a Python type plus a bound. The integer codes for the model-choice fields are the same integers MAGEC's settings sheet uses; the citation map below is lifted from `MAGECConfig`'s field comments and the Sun & Yao 2024 supplement.

| volcatenate YAML key | Default           | Allowed values (with type)                                                                                                                                                                                                   | Behavior                                                                                                                                                                                                                                                                                                                                                | MATLAB struct field / CSV column                          |
| -------------------- | ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| `solver_dir`       | (auto-detected)   | `str`: filesystem path                                                                                                                                                                                                     | Path to the directory containing `MAGEC_Solver_v1b.p`. Auto-detected at import time; set explicitly if detection fails. Set the `MAGEC_SOLVER_DIR` env var to override globally.                                                                                                                                                                    | (added to MATLAB path)                                    |
| `matlab_bin`       | (auto-detected)   | `str`: filesystem path                                                                                                                                                                                                     | Path to the `matlab` binary. Auto-detected from `$PATH` or common installs. Set the `MATLAB_BIN` env var to override globally.                                                                                                                                                                                                                    | (used in subprocess)                                      |
| `redox_option`     | `Fe3+/FeT`      | `str`: `Fe3+/FeT`, `dFMQ`, `logfO2`, `S6+/ST`                                                                                                                                                                      | Volcatenate wrapper setting. Names which redox column MAGEC reads from the input CSV. Volcatenate has direct sample sources for `Fe3+/FeT` and `dFMQ` only; `logfO2` and `S6+/ST` are valid MAGEC options but the wrapper does not currently surface them on the `MeltComposition`. See the expanded `magec.redox_source` subsection below. | input CSV `Initial redox options` column                |
| `redox_source`     | `auto`          | `str`: `auto`, `fe3fet`, `dfmq`, `dnno`, `kc91_from_buffer`                                                                                                                                                      | Volcatenate wrapper setting. Strictness of `redox_option`, with the additional `kc91_from_buffer` mode that explicitly opts into a wrapper-side Kress & Carmichael (1991) inversion of `dNNO` / `dFMQ` to `Fe3+/FeT`. See the expanded subsection below.                                                                                      | (drives `_resolve_magec_redox`)                         |
| `sulfide_sat`      | `0`             | `int`: `0` (no), `1` (yes)                                                                                                                                                                                             | Whether MAGEC enforces sulfide saturation at each step (clipping melt S to SCSS).                                                                                                                                                                                                                                                                       | `sat_sulfide`                                           |
| `sulfate_sat`      | `0`             | `int`: `0` (no), `1` (yes)                                                                                                                                                                                             | Whether MAGEC enforces sulfate (anhydrite) saturation.                                                                                                                                                                                                                                                                                                  | `sat_sulfate`                                           |
| `graphite_sat`     | `0`             | `int`: `0` (no), `1` (yes)                                                                                                                                                                                             | Whether MAGEC enforces graphite saturation.                                                                                                                                                                                                                                                                                                             | `sat_graphite`                                          |
| `fe_redox`         | `1`             | `int`: `1` (Sun & Yao (2024) EPSL — MAGEC paper), `2` (Kress & Carmichael (1991) CMP 108:82-92), `3` (Hirschmann (2022) — Fe redox at high P)                                                                      | Choice of Fe redox model (Fe3+/FeT ↔ fO2 mapping). KC91 is the same equation volcatenate uses internally for the `kc91_from_buffer` redox cascade — picking `2` here makes MAGEC's internal Fe redox like-with-like with the wrapper's conversion.                                                                                                | `Fe32_opt`                                              |
| `s_redox`          | `1`             | `int`: `1` (Sun & Yao (2024) EPSL), `2` (Nash et al. (2019) EPSL 507:187-198), `3` (Jugo (2010) Geology 38:415-418), `4` (O'Neill & Mavrogenes (2022) GCA 334:368-382), `5` (Boulliung & Wood (2023) CMP 178:56) | S6+/ST relationship as a function of fO2.                                                                                                                                                                                                                                                                                                               | `S62_opt`                                               |
| `scss`             | `1`             | `int`: `1` (Blanchard et al. (2021)), `2` (Fortin et al. (2015)), `3` (Smythe et al. (2017)), `4` (O'Neill (2021) "Magma Redox Geochemistry" ch. 10)                                                               | Sulfide content at sulfide saturation (SCSS) model.                                                                                                                                                                                                                                                                                                     | `SCSS_opt`                                              |
| `sulfide_cap`      | `1`             | `int`: `1` (Nzotta et al. (1999)), `2` (O'Neill (2021) "Magma Redox Geochemistry" ch. 10), `3` (Boulliung & Wood (2023) CMP 178:56)                                                                                  | Sulfide-capacity (Cs²⁻) model.                                                                                                                                                                                                                                                                                                                        | `S2max_opt`                                             |
| `co2_sol`          | `1`             | `int`: `1` (Iacono-Marziano et al. (2012)), `2` (Liu et al. (2005)); plus a `3.x` family of Burguisser et al. (2015) sub-options not enumerated by Sun & Yao 2024                                                    | CO2 solubility model. The `3.x` Burguisser sub-options are exposed by MAGEC's settings sheet but their integer-suffix mapping is not documented in the volcatenate config; pass through as raw integers if needed.                                                                                                                                    | `CO2_opt`                                               |
| `h2o_sol`          | `1`             | `int`: `1` (Iacono-Marziano et al. (2012)), `2` (Liu et al. (2005)); plus a `3.x` Burguisser et al. (2015) family (see `co2_sol` note)                                                                             | H2O solubility model.                                                                                                                                                                                                                                                                                                                                   | `H2O_opt`                                               |
| `co_sol`           | `1`             | `int`: `1` (Armstrong et al. (2015) GCA 171:283-302); plus a `2.x` Yoshioka et al. (2019) family                                                                                                                       | CO solubility model. Has effect only at reduced fO2 where dissolved CO is non-trivial.                                                                                                                                                                                                                                                                  | `CO_opt`                                                |
| `adiabatic`        | `0`             | `int`: `0` (isothermal — T held fixed); other values are MAGEC-internal adiabat-rate codes not currently enumerated                                                                                                     | Thermal model. Use `0` for almost all natural-degassing studies; non-zero values would model decompression-induced cooling along an adiabat.                                                                                                                                                                                                          | `adiabat_r`                                             |
| `solver`           | `2`             | `int`: `1` (lsqnonlin), `2` (fsolve)                                                                                                                                                                                   | MATLAB nonlinear solver MAGEC drives. fsolve is more robust for MAGEC's residual structure; volcatenate's default of `2` matches the Sun & Yao 2024 example configurations.                                                                                                                                                                           | `solver_opt`                                            |
| `gas_behavior`     | `1`             | `int`: `1` (real gas — fugacity coefficients applied), `2` (ideal — y = 1 for all species)                                                                                                                           | Vapor-phase non-ideality switch. Use `1` for natural-pressure runs.                                                                                                                                                                                                                                                                                   | `ideal`                                                 |
| `o2_balance`       | `0`             | `int`: `0` (total O conserved by mass balance), `1` (fO2 fixed to a buffer)                                                                                                                                            | How MAGEC handles fO2 evolution.`0` is the realistic eruptive scenario — total O is mass-balanced and fO2 evolves with the gas phase. `1` pins fO2 to a buffer regardless of degassing.                                                                                                                                                            | `buffer`                                                |
| `p_start_kbar`     | `3.0`           | `float`, `> p_final_kbar`                                                                                                                                                                                                | Starting pressure (kbar) for MAGEC's pressure grid. If the sample's saturation pressure is higher than `p_start_kbar`, MAGEC will not find saturation in the search range — MATLAB hangs and the wrapper kills it after `timeout` seconds. Increase `p_start_kbar` (or check the comp's volatile budget) when this happens.                      | input CSV `P_degas (kbar)` column (per-row)             |
| `p_final_kbar`     | `0.001`         | `float`, `> 0`, `< p_start_kbar`                                                                                                                                                                                       | Final pressure (kbar) at the bottom of the degassing path.                                                                                                                                                                                                                                                                                              | input CSV `P_final (kbar)` column                       |
| `n_steps`          | `100`           | `int`, `≥ 2`                                                                                                                                                                                                            | Number of pressure steps in the grid. Volcatenate creates a log-spaced grid from `p_start_kbar` down to `p_final_kbar` with `n_steps` points (so each row of the input CSV is one pressure on this grid). More steps = smoother curves, longer runtime.                                                                                           | (used by `np.logspace` in `_build_sample_input_rows`) |
| `timeout`          | `300` (seconds) | `int`, `≥ 1`                                                                                                                                                                                                            | Subprocess timeout in seconds. MATLAB will be killed after this; the run is then aborted and a warning logged. Batch saturation-pressure runs scale this as `timeout + 10 × n_samples` to account for MATLAB startup amortization plus per-sample solver work.                                                                                       | (subprocess.run)                                          |
| `overrides`        | `{}`            | `dict[str, dict[str, Any]]`. Example: `{"Fogo": {"p_start_kbar": 8.0}}`                                                                                                                                                  | Per-sample overrides keyed by sample name. Unknown field names are warned and ignored. See[Per-sample overrides in configuration.md](configuration.md#per-sample-overrides).                                                                                                                                                                               | (resolved in `resolve_sample_config`)                   |

### `magec.redox_source`

How MAGEC's initial redox state is set. Default `magec.redox_option = "Fe3+/FeT"`, `magec.redox_source = "auto"`. Both keys are volcatenate wrapper settings; MAGEC itself only reads the resulting `Initial redox options` / `Initial redox values` columns from the input CSV. The wrapper picks which of those four MAGEC options to populate, and what value to put there.

#### MAGEC's internal representation of redox

MAGEC's input CSV carries a per-row `Initial redox options` column (one of `Fe3+/FeT`, `dFMQ`, `logfO2`, `S6+/ST`) and a matching `Initial redox values` numeric column. MAGEC's solver reads exactly one of these per row; the chosen `magec.fe_redox` model dictates the equation MAGEC then uses to convert to its internal Fe3+/FeT representation during the run. Volcatenate writes the same redox option and value on every row of the input CSV (one row per pressure step), so the redox state is fixed at the top of the path; degassing evolution from that initial state is governed by `magec.o2_balance`.

The wrapper has direct sample sources for two of MAGEC's four redox options:

- `Fe3+/FeT` ← `comp.fe3fet_computed` (which is itself derived from the sample's `Fe3FeT`, or speciated `FeO + Fe2O3`, or the wrapper's KC91 inversion of `dFMQ` / `dNNO` if `redox_source = "kc91_from_buffer"`).
- `dFMQ` ← `comp.dFMQ` directly.

`logfO2` and `S6+/ST` are valid MAGEC inputs but volcatenate has no sample-side source for them — the wrapper does not currently expose `logfO2` or measured `S6+/ST` on `MeltComposition`. Those two MAGEC options are kept in the allowed-values list for completeness but selecting them today triggers the auto-mode fallback.

#### Disambiguation: `redox_option` vs. `redox_source` vs. `fe_redox`

These are three independent settings the user controls separately:

- `magec.redox_option` selects the **input column** — which of MAGEC's four redox columns the wrapper writes into the input CSV (`Fe3+/FeT`, `dFMQ`, `logfO2`, `S6+/ST`).
- `magec.redox_source` selects the **strictness** of that choice and whether the wrapper is allowed to perform its own KC91 conversion (`auto` / `fe3fet` / `dfmq` / `dnno` / `kc91_from_buffer`).
- `magec.fe_redox` selects the **conversion equation MAGEC itself uses** for Fe3+/FeT ↔ fO2 (`1` Sun & Yao 2024 / `2` KC91 / `3` Hirschmann 2022). It is consulted whenever MAGEC needs an Fe3+/FeT ↔ fO2 mapping during the run, regardless of which input column the wrapper populated.

Setting `redox_option = "Fe3+/FeT"` and `fe_redox = 2` (KC91) is the most internally consistent combination when the wrapper is doing its own KC91 conversion via `redox_source = "kc91_from_buffer"` — the same equation gets used for the wrapper-side conversion (at 1 bar) and for MAGEC's internal step-by-step iteration (at the run's actual P, T).

#### Per-mode behavior of `redox_source`

| `redox_source` value | Sample field used (set in CSV / dict /`MeltComposition`)                                                                  | Initial value sent to MAGEC                                                                         | Behavior when the required sample field is missing                                                                                                                                                                                                                                                                                                      |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `auto` (default)     | best available, in priority order:`redox_option` first, then `Fe3+/FeT`, then `dNNO` / `dFMQ` via wrapper-side KC91 | the matching MAGEC column (often `Fe3+/FeT`)                                                      | (1) Honors `redox_option` if the matching indicator is on the sample. (2) Else uses `Fe3+/FeT` if available (logged at INFO). (3) Else if `dNNO` or `dFMQ` is available, computes `Fe3+/FeT` via wrapper-side KC91 + Frost-1991 buffer at 1 bar — substantively different; logged at WARNING. (4) Raises `ValueError` if everything fails. |
| `fe3fet`             | `Fe3FeT` (or speciated `FeO + Fe2O3`)                                                                                   | `Fe3+/FeT`                                                                                        | Raises `ValueError`.                                                                                                                                                                                                                                                                                                                                  |
| `dfmq`               | `dFMQ`                                                                                                                    | `dFMQ` (sent directly; MAGEC reads the value into `fe_redox` per the chosen `fe_redox` model) | Raises `ValueError`.                                                                                                                                                                                                                                                                                                                                  |
| `dnno`               | (none — MAGEC does not accept dNNO directly)                                                                               | (does not run)                                                                                      | Always raises `ValueError` with a message pointing to `kc91_from_buffer` as the supported way to use a dNNO sample with MAGEC.                                                                                                                                                                                                                      |
| `kc91_from_buffer`   | `dNNO` (preferred) or `dFMQ`                                                                                            | `Fe3+/FeT` (computed by wrapper at 1 bar)                                                         | Raises `ValueError` if neither buffer indicator is on the sample, or if the KC91 inversion fails to converge. Logged at INFO when it succeeds.                                                                                                                                                                                                        |

The KC91 conversion in `auto` mode is volcatenate's own calculation, not something MAGEC does. The user asked for, e.g., `dFMQ`, but MAGEC actually receives `Fe3+/FeT` derived at 1 bar — different from MAGEC's per-step P and T. For guaranteed transparency about which indicator is reaching MAGEC, set `redox_source` to a strict mode and let the wrapper raise on missing data instead of silently substituting.

#### Output columns

The choice of `redox_option` / `redox_source` directly determines the initial values of the `logfO2`, `dFMQ`, and `Fe3Fet_m` columns in the standardized output CSV. The chosen initial state then propagates through S speciation and gas-phase composition for the rest of the run, modulated by `magec.fe_redox` and `magec.o2_balance`.

#### Initialization vs. evolution

`redox_option` / `redox_source` only fix the starting state at the top of the degassing path (volcatenate writes the same value on every input-CSV row, but MAGEC uses only the first row's redox to seed the run). Whether fO2 evolves with pressure during degassing is gated by `magec.o2_balance`: `0` (default) lets fO2 evolve from the total-oxygen mass balance; `1` pins it to a fixed buffer.

#### Which other MAGEC YAML keys interact with these

`magec.redox_option` and `magec.redox_source` are the dispatch switches for redox initialization. `magec.fe_redox` controls the equation used for Fe3+/FeT ↔ fO2 inside MAGEC; `magec.o2_balance` controls evolution. For the complete map of which keys are read in which mode and which are ignored, see [fO2-related YAML keys: precedence and which keys are ignored when others are set](#fo2-related-yaml-keys-precedence-and-which-keys-are-ignored-when-others-are-set) in the universal Redox section above.

### Wrapper-managed values not in the YAML — MAGEC

These are sent to MAGEC by volcatenate but are not in the YAML. They are either hardcoded in `_build_sample_input_rows` in [backends/magec.py](../src/volcatenate/backends/magec.py), pulled directly from the `MeltComposition`, or computed from the sample.

| MAGEC field / CSV column                                                                | volcatenate value                                                             | Meaning                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `Reference`                                                                           | `"auto_satP"`                                                               | Tells MAGEC to compute the saturation pressure itself rather than use a user-supplied reference P. The alternate "referenced" mode (where the user would supply an externally-determined initial pressure via `Reference P (kbar)`, e.g., a melt-inclusion entrapment pressure) is not surfaced because volcatenate always wants auto-satP.                                                                                                    |
| `Reference P (kbar)`                                                                  | `np.nan`                                                                    | Always `NaN`; only consulted when `Reference != "auto_satP"`.                                                                                                                                                                                                                                                                                                                                                                                |
| `Run_ID`                                                                              | `"<sample>_<step>"`                                                         | Per-row run identifier MAGEC echoes back in its output. Used by the wrapper to split batch outputs by sample.                                                                                                                                                                                                                                                                                                                                    |
| `T_degas (C)`, `P_final (kbar)`                                                     | from `MeltComposition.T_C` and `cfg.p_final_kbar`                         | Per-row scalar inputs.                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `melt_SiO2 / TiO2 / Al2O3 / Cr2O3 / FeOT / MgO / MnO / CaO / Na2O / K2O / P2O5 (wt%)` | from `MeltComposition`, anhydrous-renormalized                              | All major oxides (including `Cr2O3`, which is a real `MeltComposition` field — defaults to 0 if absent on the CSV). MAGEC expects oxide totals on a volatile-free basis summing to 100 wt%, so the wrapper computes `norm = 100 / sum_anhydrous_oxides` and multiplies every oxide by `norm` before writing the input row.                                                                                                              |
| `Bulk_H (wt%)`, `Bulk_C (wt%)`, `Bulk_S (wt%)`                                    | computed:`H2O × norm × (2/18)`, `CO2 × norm × (12/44)`, `S × norm` | MAGEC expects elemental volatiles (H, C, S) per 100 g of anhydrous melt — not molecular H2O / CO2 / S. The wrapper does the molecular → elemental conversion using rounded molecular weights (`2/18` for H2O → H, `12/44` for CO2 → C) that match the convention used by Sun & Yao 2024 in their `example1.xlsx` and supplement files. **MAGEC does not perform this conversion internally**; the wrapper is responsible for it. |
| `Initial redox options`, `Initial redox values`                                     | from `MeltComposition` via `_resolve_magec_redox`                         | Exactly one redox column per row, picked by `magec.redox_option` and `magec.redox_source` per the deep-dive subsection above. Volcatenate writes the same redox value on every row of the input CSV.                                                                                                                                                                                                                                         |

The pressure grid sent to MAGEC is log-spaced (not linear) from `cfg.p_start_kbar` down to `cfg.p_final_kbar` with `cfg.n_steps` points; this is computed by `np.logspace` inside `_build_sample_input_rows`.

<!-- PENDING(user): Several MAGEC option-integer citations are short ("Author (year)") because the volcatenate config comments encode them tersely and MAGEC's source is a compiled .p file with no docstring citations. Specifically: Hirschmann (2022), Nash et al. (2019), Jugo (2010), Blanchard et al. (2021), Fortin et al. (2015), Smythe et al. (2017), Nzotta et al. (1999), Iacono-Marziano et al. (2012), Liu et al. (2005), Burguisser et al. (2015), Yoshioka et al. (2019). Worth a pass against the Sun & Yao (2024) supplement (Volatile_Models/MAGEC24/Supplement/) to upgrade these to journal+pages format if you want consistency with the EVo / VolFe rows. -->

<!-- PENDING(user): The `co2_sol`, `h2o_sol`, `co_sol` Burguisser (3.x) and Yoshioka (2.x) sub-options exist in MAGEC's settings sheet but are not enumerated in the volcatenate `MAGECConfig` comments — they pass through as raw integers. If you want the full sub-option map (e.g. 3.1 = Burguisser sub-A, 3.2 = sub-B), it would need to be lifted from the MAGEC settings sheet inside `Table S4.xlsm` or `default_config.yaml` in the supplement. Flagged here in case it matters for the worked-fO2-examples section. -->

---

## SulfurX

SulfurX is a Python C-O-H-S degassing model that bundles an Iacono-Marziano (or VolatileCalc) C-O-H solubility front-end with its own sulfur-saturation and redox-evolution machinery. It is not pip-installable — its source must be on `sys.path` via `config.sulfurx.path` (auto-detected; settable via the `SULFURX_PATH` env var). Volcatenate calls into SulfurX's internal modules directly (`Iacono_Marziano_COH`, `degassingrun`, `OxygenFugacity`, `Sulfur_Iron`, `Sulfur_Saturation`, etc.) rather than invoking a single entry point. SulfurX prints to stdout during the satP search and the per-step degassing solver; the wrapper's `_quiet_sulfurx()` context routes that to the volcatenate logger at DEBUG.

### Files saved per SulfurX run

SulfurX is called inline (no subprocess, no script files), so unlike EVo, VolFe, or MAGEC it does not produce any per-run intermediate files on disk. For one degassing run on a sample called `Fuego`, the on-disk layout is therefore minimal:

```
<output_dir>/
├── SulfurX/
│   └── fuego.csv                              # standardized degassing path (always)
├── saturation_pressures.csv                   # row for each (sample, model) (when running calculate_saturation_pressure)
├── resolved_inputs/
│   └── Fuego/
│       └── SulfurX.yaml                       # exact composition, T_K, volatiles, delta_FMQ, params, sulfide phase comp (when save_bundle is on)
└── <save_bundle>.json                         # full run bundle including resolved_inputs (when save_bundle is set)
```

There is no `<raw_output_dir>/sulfurx/Fuego/` subtree because the wrapper does not write any raw artifacts; `keep_raw_output: true` therefore has no effect on SulfurX runs.

The same on-disk layout is documented in the [Top-level RunConfig settings](#on-disk-layout-the-top-level-fields-produce) section above; the file list here is the SulfurX-specific subset.

### How the YAML propagates

```
volcatenate_config.yaml
  └── sulfurx:
       └── RunConfig.sulfurx (SulfurXConfig dataclass)
            └── _run_degassing()
                 ├── _find_saturation_pressure_im()  → robust satP
                 ├── _compute_delta_fmq()            → fO2 conversion (fixed cascade)
                 ├── _patch_composition()            → monkey-patches SulfurX's
                 │                                     hardcoded MeltComposition
                 └── COHS_degassing.degassing_redox / degassing_noredox
                      → row-by-row results DataFrame
```

Wrapper code: [backends/sulfurx.py](../src/volcatenate/backends/sulfurx.py).

### Field-by-field — SulfurX

The "SulfurX variable" column gives the name SulfurX itself uses for the value (a constructor argument, an attribute on a degassing object, or in some cases a method-dispatch switch). Defaults are explicit. Allowed values are enumerated when finite; for continuous fields the column gives a Python type plus a bound. Several SulfurX YAML keys are integers in {0, 1} that correspond to discrete model choices, not numeric tunables — those are called out in the Behavior column.

| volcatenate YAML key   | Default         | Allowed values (with type)                                                                                                                                                                              | Behavior                                                                                                                                                                                                                                                                                                                                                                       | SulfurX variable                                                                 |
| ---------------------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------- |
| `path`               | (auto-detected) | `str`: filesystem path                                                                                                                                                                                | Filesystem path to the SulfurX source directory. Auto-detected at import time; set explicitly if detection fails. Set the `SULFURX_PATH` env var to override globally.                                                                                                                                                                                                       | (added to `sys.path`)                                                          |
| `coh_model`          | `0`           | `int`: `0` (Iacono-Marziano et al. (2012) Chem. Geol. 334:317-330 — IM solubility model), `1` (Newman & Lowenstern (2002) Computers & Geosciences 28(5):597-604 — VolatileCalc)                 | Which C-O-H solubility front-end SulfurX uses to find saturation and step volatiles. The two parameterizations behave very differently outside basaltic compositions; in particular, the IM 2012 calibration is the appropriate choice for the basalt suite this paper uses.                                                                                                   | `COH_model`                                                                    |
| `slope_h2o`          | `-0.3396`     | `float`                                                                                                                                                                                               | Slope of the K2O-vs-H2O regression `K2O = slope * H2O + constant` used inside Iacono-Marziano. Default calibrated for Etna-like compositions. If the sample is far from that calibration set (e.g. a MORB), this coefficient may be wrong; consult the IM 2012 paper.                                                                                                        | `a` (passed to `IaconoMarziano(a=…)`)                                       |
| `constant_h2o`       | `2.7`         | `float`                                                                                                                                                                                               | Intercept of the same K2O-vs-H2O regression.                                                                                                                                                                                                                                                                                                                                   | `b` (passed to `IaconoMarziano(b=…)`)                                       |
| `n_steps`            | `600`         | `int`, `≥ 2`                                                                                                                                                                                       | Number of pressure-grid points from saturation pressure down to ~0. SulfurX's degassing loop runs once per step, and each step's inner solver can run up to 100k iterations, so `n_steps` is the dominant runtime knob. More steps → smoother curves and longer runs. The SulfurX documentation says "Please make sure the pressure step is small enough."                  | `l`                                                                            |
| `n_runs`             | `600`         | `int`                                                                                                                                                                                                 | Sets whether a degassing run will go down to 1 bar. This is not yet checked against the actual use within the SulfurX code. The SulfurX documentation states "total numbers of runs along degassing. This decides if the degassing model run till 1bar or not." 600 is the default value, and we suggest not changing this.                                                    | `m`                                                                            |
| `fo2_tracker`        | `1`           | `int`: `0` (buffered: melt held at fixed dFMQ throughout), `1` (redox evolves: per-step electron balance is tracked and melt redox responds to S and Fe degassing)                                | Switches which SulfurX degassing routine is called per pressure step:`0` calls `degassing_noredox(delta_FMQ=…)`, which keeps fO2 pinned to the initial dFMQ at every step; `1` calls `degassing_redox(e_balance_initial=…, sigma=…)`, which lets fO2 evolve from total-electron mass balance. Use `1` for the realistic eruptive scenario; `0` is a diagnostic. | (selects `degassing_redox` vs. `degassing_noredox`)                          |
| `monte_carlo`        | `0`           | `int`: `0` (no Monte Carlo simulation is performed), `1` (Monte Carlo simulation is performed)                                                                                                   | If `1`, runs a Monte Carlo simulation to estimate S degassing error generated by the errors in partition coefficients (Kds); number of runs required. The SulfurX documenation notes, "This part can take time depending on the number of runs." If `0`, no Monte Carlo is performed (pass).                                                                               | `monte_carlo`                                                                  |
| `monte_carlo_n_iter` | `0`           | `int`                                                                                                                                                                                                 | Sets the number of iterations performed by the Monte Carlo simulation. Only used if `monte_carlo` = `1`.                                                                                                                                                                                                                                                                   | `m_run`                                                                        |
| `s_fe_choice`        | `1`           | `int`: `0` (Nash et al. (2019) EPSL 507:187-198 — Nash S6+/ST), `1` (O'Neill & Mavrogenes (2022) GCA 334:368-382 — O'Neill S6+/ST)                                                              | Selects the S6+/ST speciation model SulfurX uses inside both `Sulfur_Iron(...)` and the per-step degassing solver. See the "Redox initialization and S speciation" subsection below.                                                                                                                                                                                         | `S_Fe_choice` (in `COHS_degassing`); `model_choice` (in `Sulfur_Iron`)   |
| `sigma`              | `0.005`       | `float`, `> 0`                                                                                                                                                                                      | log10 fO2 tolerance for the per-step redox solver. Smaller = tighter convergence and longer runs. Read only when `fo2_tracker = 1`. Rarely needs changing.                                                                                                                                                                                                                   | `sigma`                                                                        |
| `sulfide_pre`        | `0`           | `int`: `0` (no sulfide phase ever forms; melt S follows degassing only), `1` (check SCSS at each step; precipitate sulfide and clip melt S when SCSS is exceeded)                                 | Sulfide-saturation switch.                                                                                                                                                                                                                                                                                                                                                     | `sulfide_pre`                                                                  |
| `crystallization`    | `0`           | `int`: `0` (no crystallization — the only path SulfurX exercises today), `1` (fractional-crystallization branch — wired through in upstream but not exercised by the comparison-paper workflow) | Crystallization switch.                                                                                                                                                                                                                                                                                                                                                        | `choice` / `xlt_choice`                                                      |
| `open_degassing`     | `0`           | `int`: `0` (closed-system degassing — vapor stays in equilibrium each step), `1` (open-system — vapor removed at each step)                                                                     | Closed- vs. open-system degassing. Wired through but lightly tested for SulfurX; closed is the default natural-eruption case.                                                                                                                                                                                                                                                  | `op` / `open_degassing`                                                      |
| `d34s_initial`       | `1.0`         | `float`                                                                                                                                                                                               | Initial bulk δ³⁴S, used when isotope tracking is wired up. Currently has no effect on the standardized output; reserved for future isotope work.                                                                                                                                                                                                                            | `d34s_m_initial`                                                               |
| `sulfide.fe`         | `65.43`       | `float`, `0 ≤ value ≤ 100`                                                                                                                                                                        | Fe weight percent**of the sulfide phase** (not of the melt). Default matches `main_Fuego.py` (a near-stoichiometric pyrrhotite). Read by SulfurX's SCSS calculations; only consulted when `sulfide_pre = 1` or when SCSS is reported in the output.                                                                                                                  | `sulfide["Fe"]` (in `Sulfur_Saturation` and the per-step `COHS_degassing`) |
| `sulfide.ni`         | `0.0`         | `float`, `0 ≤ value ≤ 100`                                                                                                                                                                        | Ni weight percent of the sulfide phase.                                                                                                                                                                                                                                                                                                                                        | `sulfide["Ni"]`                                                                |
| `sulfide.cu`         | `0.0`         | `float`, `0 ≤ value ≤ 100`                                                                                                                                                                        | Cu weight percent of the sulfide phase.                                                                                                                                                                                                                                                                                                                                        | `sulfide["Cu"]`                                                                |
| `sulfide.o`          | `0.0`         | `float`, `0 ≤ value ≤ 100`                                                                                                                                                                        | O weight percent of the sulfide phase.                                                                                                                                                                                                                                                                                                                                         | `sulfide["O"]`                                                                 |
| `sulfide.s`          | `36.47`       | `float`, `0 ≤ value ≤ 100`                                                                                                                                                                        | S weight percent of the sulfide phase. Default matches `main_Fuego.py`.                                                                                                                                                                                                                                                                                                      | `sulfide["S"]`                                                                 |
| `overrides`          | `{}`          | `dict[str, dict[str, Any]]`. Example: `{"Fogo": {"n_steps": 100, "sigma": 0.001}}`                                                                                                                  | Per-sample overrides keyed by sample name. Unknown field names are warned and ignored. Nested-dataclass fields (e.g.`sulfide.fe`) can be overridden as partial dicts — the wrapper merges them onto the parent. See [Per-sample overrides in configuration.md](configuration.md#per-sample-overrides).                                                                         | (resolved in `resolve_sample_config`)                                          |

### Redox initialization and S speciation

Unlike EVo, VolFe, and MAGEC, SulfurX has no user-facing `redox_source` setting. The wrapper always derives a single number — `delta_FMQ` — from the sample, sends it to SulfurX's `OxygenFugacity.fmq()` + `fe_ratio()` to set the initial Fe3+/FeT, and then steps from there. The closest analog to the other backends' `fo2_source` is the `s_fe_choice` field (which selects the S speciation model, not the redox source). This subsection documents the fixed redox-initialization cascade and the role of `s_fe_choice`.

#### SulfurX's internal representation of fO2

SulfurX represents the redox state of the melt internally as `delta_FMQ` (log fO2 relative to the FMQ buffer), kept on the `OxygenFugacity` object. Whatever sample field is supplied, the wrapper converts it to a single `delta_FMQ` value at the sample's T (and 1 bar) via `_compute_delta_fmq` in [backends/sulfurx.py](../src/volcatenate/backends/sulfurx.py). SulfurX then uses `OxygenFugacity.fmq()` and `OxygenFugacity.fe_ratio(fmq + delta_FMQ)` to set the initial Fe3+/FeT. The Frost (1991) FMQ expression SulfurX uses internally and the one volcatenate uses to compute `delta_FMQ` from a sample `Fe3+/FeT` are the same equation, so the two paths are internally consistent.

The fixed cascade (no YAML override is exposed):

1. If the sample has `dFMQ` → use it directly.
2. Else if it has Fe3+/FeT (from speciated `FeO + Fe2O3` or explicit `Fe3FeT`) → invert KC91 at 1 bar to get logfO2, subtract Frost-1991 FMQ at the sample's T → `delta_FMQ`. Logged at INFO.
3. Else if it has `dNNO` → convert via Frost-1991 NNO and FMQ at 1 bar: `delta_FMQ = dNNO + (NNO(T) − FMQ(T))`. Logged at INFO.
4. Else → `ValueError("SulfurX requires a redox constraint")`.

The temperature-dependent NNO − FMQ offset (≈ 0.74 at 1200 °C, 0.75 at 1030 °C) is computed from the Frost-1991 expressions; an older approximation that used a constant 0.7 was systematically wrong by ~0.04–0.05 log units and inconsistent with how SulfurX itself computes `delta_FMQ` from Fe3+/FeT. The KC91 inversion uses Brent's method (`scipy.optimize.brentq`) over the logfO2 range −25 to 0 (IW − 5 to HM + 5); on Brent failure the wrapper falls through to the dNNO path with a warning.

#### Disambiguation: `fo2_tracker` vs. `s_fe_choice`

These are two independent redox-related settings the user controls separately:

- `sulfurx.fo2_tracker` selects the **evolution mode** for fO2 during degassing (`0` = buffered at the initial `delta_FMQ` throughout, `1` = fO2 evolves from electron-balance bookkeeping). It does not change how the initial `delta_FMQ` is set.
- `sulfurx.s_fe_choice` selects the **S6+/ST speciation model** SulfurX uses to convert between sulfate / sulfide fractions and fO2: `0` (Nash et al. 2019) or `1` (O'Neill & Mavrogenes 2022). It is consulted both at initialization (in `Sulfur_Iron(...)` to compute the initial `S6+/ST` from the initial fO2 / Fe3+/FeT) and at every pressure step (inside `COHS_degassing`).

Setting `s_fe_choice = 1` (O'Neill & Mavrogenes) is the most internally consistent choice when comparing SulfurX results to MAGEC's own `s_redox = 4` (also O'Neill & Mavrogenes 2022) or to VolFe's `sulfate_sol = "ONeill22dil"` family.

#### Output columns

The cascade above directly determines the initial values of the `logfO2`, `dFMQ`, and `Fe3Fet_m` columns in the standardized output CSV. The chosen initial state then propagates through SulfurX's per-step degassing solver — modulated by `fo2_tracker` (whether fO2 evolves) and `s_fe_choice` (which equation maps S between melt and vapor).

#### Initialization vs. evolution

The cascade only fixes the starting state at the saturation pressure. Whether fO2 evolves with pressure during degassing is gated by `sulfurx.fo2_tracker`: `0` keeps `delta_FMQ` fixed at the initial value for every step; `1` (default) lets fO2 evolve from total-electron mass balance.

### Wrapper-managed values not in the YAML — SulfurX

These are sent to SulfurX by volcatenate but are not in the YAML. They are either pulled directly from the `MeltComposition`, computed by the wrapper, or applied at runtime via monkey-patching to work around SulfurX-internal hardcoded values.

| SulfurX value / behavior           | volcatenate value                                                        | Meaning                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `composition` (oxide dict)       | from `MeltComposition`, normalized to 100 wt% anhydrous                | All major oxides (`SiO2`, `TiO2`, `Al2O3`, `FeOT`, `MnO`, `MgO`, `CaO`, `Na2O`, `K2O`, `P2O5`) are read directly off the user's sample and passed as a dict to `IaconoMarziano(composition=…)`, `OxygenFugacity(composition=…)`, etc.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `tk` (temperature, Kelvin)       | `comp.T_C + 273.15`                                                    | Per-sample run temperature.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `h2o_wt`, `co2_ppm`, `s_ppm` | from `MeltComposition` (`H2O` wt%, `CO2 × 10000`, `S × 10000`) | Sample volatile budget.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `delta_FMQ`                      | computed by `_compute_delta_fmq` from the sample                       | The single redox number SulfurX uses for initialization. See "Redox initialization and S speciation" above for the cascade.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Composition monkey-patch           | always applied during the degassing loop                                 | SulfurX's `degassingrun.MeltComposition` class **hardcodes a Hawaiian basalt composition** when its `choice = 0` constructor branch is hit. Inside the degassing loop, every pressure step creates a fresh `MeltComposition(...)` and feeds it to IaconoMarziano, OxygenFugacity, etc. If the real sample composition differs from the Hawaiian basalt, the initial conditions (computed with the real composition) and the per-step calculations (using the hardcoded one) become inconsistent and SulfurX's inner solver hangs at 100k iterations. The wrapper's `_patch_composition` context replaces `degassingrun.MeltComposition` with a tiny shim that returns the volcatenate sample composition (normalized to 100 wt%) for every call inside the loop. The original class is restored on exit. |
| Robust saturation-pressure search  | `_find_saturation_pressure_im` (volcatenate's own algorithm)           | SulfurX's IM saturation-pressure solver uses a single `scipy.root` call seeded from `pressure × 10` MPa and silently returns the seed unchanged on non-convergence — fragile. The wrapper replaces it with a multi-guess strategy: 11 starting pressures (10–400 MPa) × 3 starting `XH2O_f` guesses (0.01, 0.5, 0.9) = 33 attempts; non-convergent attempts (the solver did not move from the seed) are filtered out; converged results are clustered within 200-bar windows; the lowest-pressure cluster with ≥ 2 members wins; the wrapper returns the median P_sat and XH2O_f from that cluster. This applies to both `calculate_saturation_pressure` and step 1 of the degassing run. There is no equivalent in upstream SulfurX.                                                                       |

---

## Redox Handling

Three redox indicators are used throughout this doc:

- `Fe3FeT` — the ferric ratio of total iron in the melt (Fe3+ / Fe_total). A direct measurement-derived number.
- `dFMQ` — log fO2 relative to the FMQ (fayalite-magnetite-quartz) buffer at the same T (and usually 1 bar).
- `dNNO` — log fO2 relative to the NNO (nickel-nickel oxide) buffer at the same T.

The numerical redox value (`Fe3FeT`, `dNNO`, or `dFMQ`) is data — it lives on the sample, alongside H2O and CO2. The YAML cannot substitute its own redox value. What the YAML controls is which of those values each backend uses.

Supply exactly one of `Fe3FeT`, `dNNO`, or `dFMQ` on each sample. (Providing more than one is technically allowed but only one is used per backend per run; the wrapper picks one according to the rules below, so there is no benefit to supplying several.)

Each backend has YAML settings that select which sample field to use:

| YAML setting                                      | What it does                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `evo.fo2_source`                                | `auto` (default): use `Fe3FeT` if on the sample; otherwise fall back to `dNNO`, then `dFMQ`. The strict modes `fe3fet` / `buffer` / `absolute` require a specific sample field and raise `ValueError` if it is absent — see the `evo.fo2_source` subsection in the EVo section below for the full per-mode rules.      |
| `volfe.fo2_column` and `volfe.fo2_source`     | Both are volcatenate wrapper settings, not native VolFe options.`fo2_column` names the preferred sample field (`Fe3FeT` / `DNNO` / `DFMQ`). `fo2_source=auto` allows fallback through the other two if the preferred field is absent. Strict modes (`fe3fet` / `dnno` / `dfmq`) require the named field; raise if absent. |
| `magec.redox_option` and `magec.redox_source` | `redox_option` is a native MAGEC option; `redox_source` is a volcatenate wrapper setting. `redox_option` names the preferred sample field. `redox_source=auto` allows fallback, including a KC91 conversion from `dNNO`/`dFMQ` to `Fe3FeT`. Strict modes raise on missing data.                                             |
| (SulfurX)                                         | No volcatenate setting. SulfurX tries `dFMQ` first, then `Fe3FeT` (via KC91 inversion at 1 bar), then `dNNO` (via Frost-1991 buffer offset).                                                                                                                                                                                        |

If you set `evo.fo2_source: fe3fet` but your sample has only `dFMQ`, EVo raises `ValueError` rather than silently substituting `dFMQ`. Strict modes exist to fail loudly when a batch run encounters an unexpected sample.

The decision tree:

```{mermaid}
flowchart TD
    Start["Each sample arrives at a backend"]
    Q1{"Does the YAML's<br/>fo2_source / redox_source<br/>name a specific indicator?"}
    Strict["Strict mode<br/>(fe3fet / buffer / dnno / dfmq /<br/>absolute / kc91_from_buffer)"]
    SAvail{"Sample carries<br/>that indicator?"}
    Auto["auto mode<br/>(default for every backend)"]
    Cascade["Wrapper tries the indicators in<br/>backend-specific priority order;<br/>falls back if the preferred one<br/>is missing on the sample. Logs<br/>the choice."]
    Use["Use that value"]
    Raise["Raise ValueError"]

    Start --> Q1
    Q1 -- "yes" --> Strict --> SAvail
    Q1 -- "no, auto" --> Auto --> Cascade --> Use
    SAvail -- "yes" --> Use
    SAvail -- "no" --> Raise
```

For the full per-backend redox cascade — including which auto-mode fallbacks are silent vs. logged at INFO vs. logged at WARNING — see the per-backend redox / fO2 deep-dive subsection (`evo.fo2_source`, `volfe.fo2_column` / `volfe.fo2_source`, `magec.redox_source`, or "Redox initialization and S speciation" for SulfurX).

### Comparing the same sample at different hypothetical redox states

There is no YAML setting that lets you override a sample's measured `Fe3FeT` / `dNNO` / `dFMQ` from the config file — the redox value lives on the sample, and the YAML only chooses *which* sample field to use. To run the same melt composition under two different hypothetical redox states (e.g., the measured `Fe3FeT = 0.18` and a hypothetical `dFMQ = 0`), you have two options:

- **Two `MeltComposition` instances.** Construct two `MeltComposition` objects with the same major-oxide and volatile content but different sample names and different redox fields, then pass both to `run_comparison`. This is the cleanest path when you are driving volcatenate from a notebook or script.
- **Two rows in the sample CSV.** Duplicate the sample row in your CSV with a different `Sample` name (e.g., `"Fuego"` and `"Fuego_dFMQ0"`) and set the redox field accordingly on each row. Both rows then become separate samples in the run, so `overrides` is not needed.

Per-sample `overrides` in the YAML (`evo.overrides`, `volfe.overrides`, etc.) operate on **YAML config fields** for a named sample, not on sample data fields. Use them to point a single sample at a different solubility model or pressure range, not to substitute a different redox value.

### fO2-related YAML keys: precedence and which keys are ignored when others are set

Every backend has multiple YAML keys related to fO2 initialization. Some are mutually exclusive; some are conditionally consulted depending on other keys. The tables below give the complete picture per backend: when each key is read, what overrides it, and which keys are silently ignored in each scenario.

In all cases, the **value** of the redox indicator is read from the sample (`Fe3FeT`, `dNNO`, or `dFMQ` on the `MeltComposition`); the YAML keys below decide which of those sample fields the wrapper uses.

#### Redox: EVo

The dispatch entry point is `evo.fo2_source`. Everything else is conditional on its value.

| YAML key                                                                             | Read when...                                                                                                                                                                                                                                                | Ignored when...                                                                                                         |
| ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| `evo.fo2_source`                                                                   | Always. The dispatch switch. Default `auto`.                                                                                                                                                                                                              | (never ignored)                                                                                                         |
| `evo.fo2_buffer`                                                                   | (a)`fo2_source: buffer` — names the buffer (`FMQ` / `NNO` / `IW`); the wrapper requires the matching offset on the sample. (b) `fo2_source: auto` AND no `Fe3FeT`/`dNNO`/`dFMQ` on the sample — used as the final fallback with offset 0. | `fo2_source` is `fe3fet` or `absolute`, OR `fo2_source: auto` and the sample carries any redox indicator.       |
| `evo.fo2_set`, `evo.fo2_start`                                                   | `fo2_source: absolute` only. `fo2_set` must be `true` and `fo2_start > 0`; raises otherwise.                                                                                                                                                        | Any other value of `fo2_source`. (Volcatenate writes them to `env.yaml` regardless, but EVo does not consult them.) |
| `evo.fo2_model`                                                                    | Whenever the Fe3+/FeT pathway is taken:`fo2_source: fe3fet`, OR `fo2_source: auto` and Fe3+/FeT or speciated FeO + Fe2O3 is on the sample. EVo also uses it in `absolute` mode to back-compute Fe3+/FeT from the imposed fO2.                         | (effectively never — used in every common path)                                                                        |
| `evo.fmq_model`                                                                    | Whenever a buffer pathway is taken:`fo2_source: buffer`, OR `fo2_source: auto` and the wrapper is using `dNNO`/`dFMQ` from the sample. Also used whenever `dFMQ` is reported in output.                                                           | (effectively never)                                                                                                     |
| `evo.fe_system`                                                                    | Always — gates whether fO2 evolves with pressure during degassing. Independent of `fo2_source`, which only sets the *initial* fO2.                                                                                                                     | (never ignored)                                                                                                         |
| `evo.fh2_set`, `evo.fh2o_set`, `evo.fco2_set` (and their `*_start` partners) | When the corresponding `_set` is `true`. These are independent fugacity entry points and are not gated by `fo2_source`.                                                                                                                               | When the corresponding `_set` is `false` (the default).                                                             |

#### Redox: VolFe

The dispatch entry point is `volfe.fo2_source`; `volfe.fo2_column` names the preferred column. Both are volcatenate wrapper settings, not native VolFe options.

| YAML key                                   | Read when...                                                                                                                                                                              | Ignored when...                                                                                                               |
| ------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `volfe.fo2_column`                       | Always — names the preferred sample field (`Fe3FeT` / `DNNO` / `DFMQ`).                                                                                                            | (never ignored as a preference, but the field it names is replaced via fallback or strict failure as `fo2_source` dictates) |
| `volfe.fo2_source`                       | Always.`auto` allows fallback through the other two sample fields if the preferred one is absent. Strict modes (`fe3fet`/`dnno`/`dfmq`) require the named field; raise if absent. | (never ignored)                                                                                                               |
| `volfe.fo2_model`                        | Whenever VolFe needs an fO2 ↔ Fe3+/FeT conversion (most paths).                                                                                                                          | (effectively never)                                                                                                           |
| `volfe.fmq_buffer`, `volfe.nno_buffer` | Whenever VolFe needs to compute fO2 relative to FMQ or NNO buffers.                                                                                                                       | (effectively never)                                                                                                           |
| `volfe.eq_fe`                            | Always — gates Fe redox equilibration during degassing. Independent of which sample field was used to initialize fO2.                                                                    | (never ignored)                                                                                                               |

#### Redox: MAGEC

Both dispatch entry points (`magec.redox_source` and `magec.redox_option`) are volcatenate wrapper settings; MAGEC itself only reads the resulting `Initial redox options` / `Initial redox values` columns from the per-row input CSV. `redox_option` names which of MAGEC's four input columns gets populated; `redox_source` controls how strictly that choice is enforced and whether the wrapper performs its own KC91 conversion.

| YAML key               | Read when...                                                                                                                                                                                                                                                 | Ignored when...                 |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------- |
| `magec.redox_option` | Always — names which MAGEC redox column to populate (`Fe3+/FeT`, `dFMQ`, `logfO2`, or `S6+/ST`).                                                                                                                                                    | (never ignored as a preference) |
| `magec.redox_source` | Always.`auto` allows fallback (including a KC91 conversion from `dNNO`/`dFMQ` to `Fe3+/FeT` when `redox_option` requires `Fe3+/FeT` and only buffer-relative redox is on the sample). Strict modes raise if the matching sample field is absent. | (never ignored)                 |
| `magec.fe_redox`     | Always — selects MAGEC's internal Fe-redox model (Sun & Yao 2024, KC91, or Hirschmann 2022).                                                                                                                                                                | (never ignored)                 |
| `magec.o2_balance`   | Always —`0` lets fO2 evolve from total O balance; `1` fixes fO2 to a buffer.                                                                                                                                                                            | (never ignored)                 |

#### Redox: SulfurX

SulfurX has no volcatenate wrapper setting for redox source. The wrapper always tries the sample fields in this order: `dFMQ` → `Fe3FeT` (via KC91 inversion at 1 bar) → `dNNO` (via Frost-1991 buffer offset). Raises `ValueError` if none are present.

There is no YAML way to override this priority for SulfurX. The native SulfurX setting `s_fe_choice` selects the S speciation model (not the redox source).

#### Where each per-backend section discusses these keys in detail

- EVo: see the `evo.fo2_source` subsection.
- VolFe: see the `volfe.fo2_column` and `volfe.fo2_source` subsection.
- MAGEC: see the `magec.redox_source` subsection.
- SulfurX: see the "Redox initialization and S speciation" subsection (no `redox_source` setting; the cascade is fixed).

`evo.nitrogen_set: False` (the default) bypasses nitrogen entirely. Every other piece of sample data has no YAML fallback — if it's not on the sample, it's not in the run.

### The YAML configuration file

Solubility models, equilibrium-constant parameterizations, fugacity-coefficient choices, gas-system identity, pressure-stepping, run-bundle paths, log-file paths, model versions — these are all pure method/output choices and don't depend on what's in the sample. The CSV and `MeltComposition` have no fields for them. The full inventory is in the per-backend sections below and in the [top-level RunConfig settings](#top-level-runconfig-settings) section above.

#### YAML syntax notes

The YAML file uses YAML syntax, not Python syntax. A few small differences worth noting:

- Booleans are `true` and `false` in lowercase — not Python's `True` / `False`. (YAML accepts the capitalised forms too as aliases, but the lowercase form is canonical and is what volcatenate writes when you call `save_config`.)
- `null` (not `None`) for explicit nulls. Empty strings, empty dicts (`{}`), and empty lists (`[]`) work as written.
- Strings do not need quotes unless they would be parsed as another type (`"yes"` quoted to keep it a string rather than a boolean; `"1"` quoted to keep it a string rather than an int). Most volcatenate string fields don't need quotes.
- Dataclass field names map 1-to-1 to YAML keys (e.g. `volfe.fo2_column` in this doc = the `fo2_column` key under the `volfe:` section in your YAML).

---

## `volcatenate` Conventions

### Unit conventions

Sample inputs are in **wt%** for every major oxide and for `H2O`, `CO2`, and `S` (the canonical `MeltComposition` fields use these units). The one exception is nitrogen, which is in **ppm** (`N_ppm`) because typical magmatic N contents are too small for wt% to be a natural unit.

Each backend internally uses different units; the wrapper handles the conversion before passing values to the model:

- VolFe and SulfurX consume CO₂ and S as ppm. The wrapper multiplies your wt% values by 10000 before passing them in.
- EVo consumes volatiles as mass fractions. The wrapper divides your wt% values by 100.
- H₂O is passed as wt% directly to backends that want it, then divided by 100 for EVo.

These rescalings preserve the underlying chemistry — they're the format each backend's input expects. You don't need to think about them when supplying the sample.

### Logging

The volcatenate log file (`RunConfig.log_file`) is created on the first call and appended with information from any calls thereafter. Assuming a log filename is supplied in the yaml, multiple `calculate_*` calls in the same notebook or script accumulate into one log instead of overwriting each other. To restart from a clean file mid-session, call `volcatenate.log.reset_log_file_tracking()` before the next call.

### Output CSV schema

The DataFrames returned by `calculate_degassing` contain a fixed set of columns ([`columns.STANDARD_COLUMNS`](../src/volcatenate/columns.py)). When backends produce extra intermediate columns (e.g. MAGEC's `Run_ID`, `_sample`), those are stripped on write via [`convert.to_standard_schema`](../src/volcatenate/convert.py). Set `keep_raw_output` to True in the configuration yaml to additionally save outputs as created directly by the backend tools with no edits by volcatenate.

---

## Worked Examples — fO2 initialization

Five common scenarios showing how each backend handles redox initialization. For all examples, the sample is a generic basalt at `T_C = 1100` (so `T_K = 1373.15`) and the run starts at saturation pressure near 1 bar. The Frost-1991 buffer values at this T are reused throughout:

| Buffer     | log₁₀ fO2 |
| ---------- | ----------- |
| FMQ        | −9.541     |
| NNO        | −8.795     |
| NNO − FMQ | +0.746      |

The Kress & Carmichael (1991) Eq. A-5/A-6 (KC91A) Fe3+/FeT ↔ logfO2 mapping is composition-dependent and is shown symbolically as `KC91A(Fe3FeT, T, P, comp)` (or its inverse). The numerical answers in the tables below are exact for the buffer expressions; KC91A values are symbolic because they depend on the full melt composition.

### Example 1 — sample has only `Fe3FeT`, all backends in `auto` mode

(Most common case for melt-inclusion data.)

**Sample data** (in CSV / `MeltComposition` / dict):

- `Sample = "Fuego"`, `T_C = 1100`
- All major oxides (basalt-like)
- `H2O`, `CO2`, `S` (volatile budget)
- `Fe3FeT = 0.18`
- `dNNO`, `dFMQ` not set

**YAML config** (defaults shown):

```yaml
evo:
  fo2_source: auto
volfe:
  fo2_column: Fe3FeT
  fo2_source: auto
magec:
  redox_option: Fe3+/FeT
  redox_source: auto
sulfurx: {}
```

**Per-backend behavior:**

| Backend | Indicator sent to model                                                                            | Conversion path                                                                                                                     | Initial `Fe3Fet_m`                | Initial `logfO2`                             | Initial `dFMQ`                               |
| ------- | -------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | ---------------------------------------------- | ---------------------------------------------- |
| EVo     | `chem.yaml` carries the `FeOT → FeO + Fe2O3` split per `Fe3FeT = 0.18`; `FO2_SET = False` | EVo computes `sys.FO2 = ln(fO2)` from Fe3+/FeT via KC91A (`evo.fo2_model = kc1991`)                                             | 0.18                                | KC91A(0.18, 1373.15, P, comp)                  | logfO2 − FMQ(T, P)                            |
| VolFe   | `setup_df["Fe3FeT"] = 0.18`                                                                      | VolFe sets `Fe3FeT_i = 0.18` directly; per-step uses `mdv.f_O2` (default `volfe.fo2_model = Kress91A`) when reporting         | 0.18                                | KC91A(0.18, 1373.15, P, comp)                  | logfO2 − FMQ(T, P)                            |
| MAGEC   | input CSV:`Initial redox options = "Fe3+/FeT"`, `Initial redox values = 0.18`                  | MAGEC applies its own Fe3+/FeT ↔ fO2 model (`magec.fe_redox = 1`, Sun & Yao 2024 by default) at run time                         | 0.18                                | Sun-Yao-2024(0.18, 1373.15, P, comp)           | logfO2 − FMQ(T, P)                            |
| SulfurX | `delta_FMQ` = wrapper-side KC91 inversion of 0.18 at 1 bar, then subtract Frost-1991 FMQ at T    | SulfurX uses `delta_FMQ` to set initial Fe3+/FeT via `OxygenFugacity.fe_ratio(fmq + delta_FMQ)` (round-trips ~0.18 numerically) | ≈ 0.18 (numerically round-tripped) | KC91A⁻¹(0.18, 1373.15,**1 bar**, comp) | KC91A⁻¹(0.18, 1373.15, 1, comp) − FMQ(T, 1) |

All four backends start at the same redox state, modulo (i) which Fe3+/FeT ↔ fO2 equation each uses internally (KC91A vs. Sun-Yao-2024) and (ii) the pressure used for the wrapper-side Fe3+/FeT ↔ fO2 conversion (1 bar in SulfurX, run-pressure elsewhere).

### Example 2 — sample has only `dNNO`, all backends in `auto` mode

(Per-backend behavior diverges here.)

**Sample data:**

- `Sample = "Sample-A"`, `T_C = 1100`, basalt oxides, volatiles
- `dNNO = +0.5`
- `Fe3FeT`, `dFMQ` not set

**YAML config:** same as Example 1 (all `auto`).

**Per-backend behavior:**

| Backend | Indicator sent to model                                                                                                                         | Conversion path                                                                                                                                                                                                  | Initial `dFMQ`                                                         | Notes                                                                                                                                                                                             |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EVo     | `env.yaml`: `FO2_buffer = "NNO"`, `FO2_buffer_SET = True`, `FO2_buffer_START = 0.5`                                                     | EVo converts dNNO → log₁₀ fO2 →`sys.FO2` via Frost-1991 NNO at run T, P                                                                                                                                    | 0.5 + (NNO − FMQ) = 0.5 + 0.746 =**+1.246**                       | Wrapper picks `"NNO"` based on `comp.dNNO`, regardless of `evo.fo2_buffer` setting. Logged at INFO.                                                                                         |
| VolFe   | `setup_df["DNNO"] = 0.5` (because `fo2_column = Fe3FeT` is missing on the sample, the cascade Fe3FeT → dNNO → dFMQ falls through to dNNO) | VolFe converts dNNO → fO2 via Frost-1991 NNO (`volfe.nno_buffer = Frost91`); then fO2 → `Fe3FeT_i` via `volfe.fo2_model = Kress91A`                                                                      | **+1.246**                                                         | Logged at INFO ("falling back to DNNO").                                                                                                                                                          |
| MAGEC   | input CSV:`Initial redox options = "Fe3+/FeT"`, `Initial redox values = KC91⁻¹(NNO + 0.5)`                                                | Wrapper computes `Fe3+/FeT` via wrapper-side KC91 inversion at **1 bar** (not run P), Frost-1991 NNO at T. MAGEC then re-applies its own `magec.fe_redox` model at run P, T.                           | **+1.246** (initial), but the value MAGEC receives is `Fe3+/FeT` | **Logged at WARNING.** The pointer `redox_source = "kc91_from_buffer"` is named in the warning message to make the choice explicit. Substantively different — MAGEC never sees `dNNO`. |
| SulfurX | `delta_FMQ = +1.246` directly                                                                                                                 | Cascade priority `dFMQ → Fe3FeT → dNNO`; sample has only dNNO, hits priority 3. Computes `delta_FMQ = dNNO + (NNO − FMQ)` at 1 bar. SulfurX then sets initial Fe3+/FeT via `fe_ratio(fmq + delta_FMQ)`. | **+1.246**                                                         | Logged at INFO.                                                                                                                                                                                   |

Three backends arrive at `dFMQ ≈ +1.246` cleanly; MAGEC's wrapper-side KC91 inversion in `auto` mode is the divergent case — the wrapper converts `dNNO` to a `Fe3+/FeT` value at 1 bar before MAGEC ever sees the redox state, which is why the wrapper logs at WARNING and points at `redox_source = "kc91_from_buffer"`.

### Example 3 — sample has both `Fe3FeT` and `dFMQ`, all backends in `auto` mode

(SulfurX's cascade priority diverges from the other three.)

**Sample data:**

- `Sample = "Sample-B"`, `T_C = 1100`, basalt oxides, volatiles
- `Fe3FeT = 0.18` AND `dFMQ = +1.246`

**YAML config:** same as Example 1 (all `auto`).

**Per-backend behavior:**

| Backend | Wins by priority                                                            | Indicator sent to model                                             | Comment                                                                                                                             |
| ------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| EVo     | `Fe3FeT` (priority 1 in EVo's `auto` cascade Fe3FeT → dNNO → dFMQ)    | speciated `FeO + Fe2O3` in `chem.yaml`                          | dFMQ on the sample is ignored.                                                                                                      |
| VolFe   | `Fe3FeT` (matches default `fo2_column = Fe3FeT`)                        | `setup_df["Fe3FeT"] = 0.18`                                       | dFMQ on the sample is ignored.                                                                                                      |
| MAGEC   | `Fe3+/FeT` (matches default `redox_option = "Fe3+/FeT"`)                | input CSV `Initial redox options = "Fe3+/FeT"`, `values = 0.18` | dFMQ on the sample is ignored.                                                                                                      |
| SulfurX | `dFMQ` (priority 1 in SulfurX's fixed cascade `dFMQ → Fe3FeT → dNNO`) | `delta_FMQ = +1.246` directly                                     | **Fe3FeT on the sample is ignored.** This is the single place where SulfurX's cascade priority diverges from the other three. |

If `Fe3FeT` and `dFMQ` are mutually consistent (both derived from the same fO2 measurement), all four backends arrive at the same redox state. If they disagree, three backends use `Fe3FeT` and SulfurX uses `dFMQ` — set only one indicator on the sample if you want byte-identical initialization across backends.

### Example 4 — sample has only `dNNO`, strict-mode `fe3fet`

(Three backends raise; SulfurX has no strict mode.)

**Sample data:** same as Example 2 (only `dNNO = +0.5`).

**YAML config:**

```yaml
evo:
  fo2_source: fe3fet
volfe:
  fo2_source: fe3fet
magec:
  redox_source: fe3fet
sulfurx: {}     # no strict-mode setting exposed
```

**Per-backend behavior:**

| Backend | Behavior                                                                                                                                                                       |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| EVo     | Raises `ValueError` ("requires Fe3+/FeT on sample, but none was provided").                                                                                                  |
| VolFe   | Raises `ValueError` ("requires Fe3+/FeT on sample, but none was provided").                                                                                                  |
| MAGEC   | Raises `ValueError` ("requires Fe3+/FeT on sample, but none was provided").                                                                                                  |
| SulfurX | No strict-mode setting exists. SulfurX's fixed cascade falls through to dNNO at priority 3, computing `delta_FMQ = +1.246` as in Example 2. **Run proceeds normally.** |

Strict modes are the right tool when running a batch where you want to fail loudly if a sample lacks the redox indicator you expect — but SulfurX cannot participate in that contract today.

### Example 5 — `evo.fo2_source = "absolute"` with `evo.fo2_start`

(Only EVo exposes an absolute-fO2 entry point.)

**Sample data:**

- `Sample = "Sample-C"`, `T_C = 1100`, basalt oxides, volatiles
- Any combination of `Fe3FeT` / `dNNO` / `dFMQ` (all ignored by EVo in `absolute` mode; the wrapper deliberately skips the FeOT split in `chem.yaml`)

**YAML config:**

```yaml
evo:
  fo2_source: absolute
  fo2_set: true
  fo2_start: 1.0e-9    # absolute fO2 in bar
volfe: {}              # no `absolute` mode in volfe.fo2_source enum (allowed: auto, fe3fet, dnno, dfmq)
magec: {}              # no `absolute` mode in magec.redox_source enum; MAGEC natively accepts logfO2 as a column, but volcatenate has no sample-side source for it
sulfurx: {}            # no equivalent
```

**Per-backend behavior:**

| Backend               | Behavior                                                                                                                                                                                                                                                                                                                                                                                             |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EVo                   | `env.yaml`: `FO2_SET = True`, `FO2_START = 1.0e-9`. EVo sets `sys.FO2 = ln(1.0e-9) ≈ −20.72`. EVo also back-computes `sys.FO2_buffer = log₁₀(1e-9) − FMQ(T, P) = −9 − (−9.541) = +0.541`. The wrapper deliberately sends `FEO = FeOT, FE2O3 = 0` in `chem.yaml` (skipping the Fe3+/FeT split) because EVo refuses to run with both an absolute fO2 and a speciated iron entry. |
| VolFe, MAGEC, SulfurX | These backends have no volcatenate-exposed absolute-fO2 entry point. To match Example 5 across backends, the user computes the equivalent `dFMQ` at the run's T, P (here `+0.541`) and places it on the sample as the `dFMQ` field, then uses Example 1-style `auto` (or the relevant strict mode) on those backends.                                                                        |

If you need consistent absolute-fO2 initialization across all four backends in a comparison run, set `dFMQ` on the sample (computed from your absolute fO2 at the run's T, P) and let the other three backends use it via their normal cascades; only EVo bypasses that route via `fo2_source = "absolute"`.

---

## Sulfur Deep-dive — Cross-backend Model Choices

The four implemented backends each carry sulfur as some combination of dissolved S²⁻ (sulfide), dissolved S⁶⁺ (sulfate), exsolved gas-phase S (S2, SO2, H2S, OCS), and a separately-tracked sulfide phase if the SCSS or SCAS is exceeded. The models that govern each piece — S²⁻ solubility / capacity, S⁶⁺ solubility / capacity, the S6+/ST relationship as a function of fO2, the sulfide saturation curve (SCSS), the sulfate saturation curve (SCAS), and whether saturation is enforced — are exposed in the YAML at different granularities and with different default citations across backends.

### What each backend tracks

- **EVo:** `S` total sulfur in the melt is split into S²⁻ (sulfide-capacity model) and S⁶⁺ (sulfate-capacity model) at every step; the gas phase carries S2, SO2, H2S, and (optionally) OCS. SCSS is the only saturation curve consulted (no SCAS path), and EVo aborts the run if the melt becomes sulfide-saturated *and* the user opted into the saturation check via `evo.s_sat_warn = true`. By default (`evo.s_sat_warn = false`) EVo runs past saturation without checking.
- **VolFe:** Tracks dissolved S²⁻ (`sulfide_sol` choice) and dissolved S⁶⁺ (`sulfate_sol` choice); the gas phase carries S2, SO2, H2S, and optionally OCS. Both SCSS and SCAS are exposed as separate model choices; the saturation check is gated by `volfe.sulfur_saturation = true` (default false), which clips melt S to the saturation curve and continues.
- **MAGEC:** Tracks total S, with the S6+/ST split governed by `magec.s_redox` and the S²⁻ component sized by `magec.sulfide_cap`. SCSS is exposed as `magec.scss`; SCAS has no YAML choice (MAGEC handles it internally). Sulfide and sulfate saturation are independently toggled by `magec.sulfide_sat` and `magec.sulfate_sat` (both default 0, off).
- **SulfurX:** Tracks dissolved S explicitly as S²⁻ + S⁶⁺ via `Sulfur_Iron`, with the S6+/ST split chosen by `sulfurx.s_fe_choice` (Nash 2019 vs. O'Neill & Mavrogenes 2022). SCSS and SCAS are hardcoded in the upstream `Sulfur_Saturation` class as Smythe et al. (2017) and Zajacz & Tsay (2019) respectively; volcatenate does not surface a YAML override. The saturation check is gated by `sulfurx.sulfide_pre = 1`.

### Cross-backend mapping: which YAML key controls each piece of S behavior

| S concept                                      | EVo                                                      | VolFe                                                           | MAGEC                                                                | SulfurX                                  |
| ---------------------------------------------- | -------------------------------------------------------- | --------------------------------------------------------------- | -------------------------------------------------------------------- | ---------------------------------------- |
| Sulfide capacity / solubility (Cs²⁻ / S²⁻) | `evo.sulfide_capacity`                                 | `volfe.sulfide_sol`                                           | `magec.sulfide_cap`                                                | (Sulfur_Iron internal; not a YAML field) |
| Sulfate capacity / solubility (Cs⁶⁺ / S⁶⁺) | `evo.sulfate_capacity`                                 | `volfe.sulfate_sol`                                           | (rolled into `magec.s_redox`)                                      | (Sulfur_Iron internal; not a YAML field) |
| S6+/ST relationship                            | implicit in `sulfide_capacity` + `sulfate_capacity`  | implicit in `sulfide_sol` + `sulfate_sol`                   | `magec.s_redox`                                                    | `sulfurx.s_fe_choice`                  |
| SCSS (sulfide saturation)                      | `evo.scss`                                             | `volfe.scss`                                                  | `magec.scss`                                                       | (Smythe17 hardcoded internally)          |
| SCAS (sulfate saturation)                      | (not exposed)                                            | `volfe.scas`                                                  | (not exposed)                                                        | (Zajacz-Tsay 2019 hardcoded internally)  |
| Saturation enforcement toggle                  | `evo.s_sat_warn` (controls *both* check and warning) | `volfe.sulfur_saturation` (and `volfe.graphite_saturation`) | `magec.sulfide_sat`, `magec.sulfate_sat`, `magec.graphite_sat` | `sulfurx.sulfide_pre`                  |

EVo's `evo.s_sat_warn` is the unusual one: when `false` (volcatenate's default), EVo never checks SCSS; when `true`, EVo checks at every step *and* terminates the run if SCSS is exceeded (rather than clipping). The other three backends decouple "check" from "terminate": VolFe and MAGEC clip-and-continue, SulfurX precipitates a sulfide phase and continues.

### Sulfide capacity / solubility — model choices

| Model family                  | EVo (`sulfide_capacity`) | VolFe (`sulfide_sol`)                                                                             | MAGEC (`sulfide_cap`) |
| ----------------------------- | -------------------------- | --------------------------------------------------------------------------------------------------- | ----------------------- |
| Nzotta et al. (1999)          | (not exposed)              | (not exposed)                                                                                       | `1` (default)         |
| O'Neill & Mavrogenes (2002)   | `oneill2002`             | (not exposed)                                                                                       | (not exposed)           |
| O'Neill (2020) / (2021)       | `oneill2020` (default)   | `ONeill21dil` (default; with H2O dilution), `ONeill21` (no dilution), `ONeill21hyd` (hydrous) | `2`                   |
| Boulliung & Wood (2023)       | (not exposed)              | `Boulliung23_eq6`, `Boulliung23_eq7`, `Boulliung23_eq7_12` (eq. 7 + P-effect)                 | `3`                   |
| Gorojovsky & Wood (pre-print) | (not exposed)              | `GorojovskyPP_eq8`, `GorojovskyPP_eq9`                                                          | (not exposed)           |
| Thomas & Wood (2026)          | (not exposed)              | `Thomas26_eq15`                                                                                   | (not exposed)           |

EVo's lineage is O'Neill 2002 / 2020; VolFe and MAGEC additionally expose Boulliung-Wood 2023. SulfurX has no YAML knob for sulfide capacity — the model is folded into `Sulfur_Iron` and `Sulfur_Saturation` upstream.

### Sulfate capacity / solubility — model choices

| Model family                     | EVo (`sulfate_capacity`) | VolFe (`sulfate_sol`)                                                                                 | MAGEC                                  |
| -------------------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------- | -------------------------------------- |
| Nash et al. (2019)               | `nash2019` (default)     | (not exposed; the Nash-2019 S6+/ST is implicit in some sulfate-capacity options)                        | (rolled into `s_redox = 2`)          |
| O'Neill & Mavrogenes (2022)      | (not exposed)              | `ONeill22dil` (default; with H2O dilution), `ONeill22` (no dilution)                                | (rolled into `s_redox = 4`)          |
| Boulliung & Wood (2022) / (2023) | (not exposed)              | `Boulliung22nP`, `Boulliung22wP`, `Boulliung23_eq9`, `Boulliung23_eq9_12`, `Boulliung23_eq11` | (rolled into `s_redox = 5`)          |
| Gorojovsky & Wood (pre-print)    | (not exposed)              | `GorojovskyPP_eq10`, `GorojovskyPP_eq11`                                                            | (not exposed)                          |
| Thomas & Wood (2026)             | (not exposed)              | `Thomas26_eq21`, `Thomas26_eq22`                                                                    | (not exposed)                          |
| Sun & Yao (2024)                 | (not exposed)              | (not exposed)                                                                                           | (rolled into `s_redox = 1`, default) |
| Jugo (2010)                      | (not exposed)              | (not exposed)                                                                                           | (rolled into `s_redox = 3`)          |

MAGEC merges its sulfate model with its S6+/ST equation into a single `magec.s_redox` integer; volcatenate documents the integer-to-citation map in the [MAGEC field-by-field table](#field-by-field-magec). SulfurX's `s_fe_choice` is the equivalent: `0` = Nash 2019, `1` = O'Neill & Mavrogenes 2022.

### S6+/ST relationship — cross-backend

EVo and VolFe compute S6+/ST implicitly as `S⁶⁺ / (S²⁻ + S⁶⁺)` from the chosen sulfide and sulfate capacity / solubility laws — there is no separate "S speciation" YAML key in those two backends. MAGEC and SulfurX expose explicit S speciation choices:

| Family                      | MAGEC (`s_redox`) | SulfurX (`s_fe_choice`) | EVo equivalent (via `sulfate_capacity`) | VolFe equivalent (via `sulfate_sol`)      |
| --------------------------- | ------------------- | ------------------------- | ----------------------------------------- | ------------------------------------------- |
| Sun & Yao (2024)            | `1` (default)     | (not exposed)             | (not exposed)                             | (not exposed)                               |
| Nash et al. (2019)          | `2`               | `0`                     | `nash2019` (default)                    | (implicit in some sulfate-capacity options) |
| Jugo (2010)                 | `3`               | (not exposed)             | (not exposed)                             | (not exposed)                               |
| O'Neill & Mavrogenes (2022) | `4`               | `1` (default)           | (not exposed)                             | `ONeill22dil` (default), `ONeill22`     |
| Boulliung & Wood (2023)     | `5`               | (not exposed)             | (not exposed)                             | `Boulliung23_eq9` etc.                    |

For a like-for-like S speciation across all four backends, O'Neill & Mavrogenes (2022) is the only family with an explicit option in MAGEC (`s_redox = 4`), VolFe (`sulfate_sol = ONeill22dil`), and SulfurX (`s_fe_choice = 1`). EVo's volcatenate default is Nash 2019 (`sulfate_capacity = nash2019`) and there is no ONeill22 sulfate-capacity option exposed for EVo today.

### SCSS — sulfide saturation models

| Model family                | EVo (`scss`)        | VolFe (`scss`)                                                           | MAGEC (`scss`) |
| --------------------------- | --------------------- | -------------------------------------------------------------------------- | ---------------- |
| Liu et al. (2007)           | `liu2007` (default) | `Liu07`                                                                  | (not exposed)    |
| Fortin et al. (2015)        | (not exposed)         | `Fortin15_pss`                                                           | `2`            |
| Smythe et al. (2017)        | (not exposed)         | `Smythe17_pss`                                                           | `3`            |
| O'Neill (2021)              | (not exposed)         | `ONeill21hyd` (default), `ONeill21`, `ONeill21dil`, `ONeill21_pss` | `4`            |
| Blanchard et al. (2021)     | (not exposed)         | `Blanchard21_eq11_pss`, `Blanchard21_eq12_pss`                         | `1` (default)  |
| Liu et al. (2021)           | (not exposed)         | `Liu21_pss`                                                              | (not exposed)    |
| O'Neill & Mavrogenes (2022) | (not exposed)         | `ONeill22_pss`                                                           | (not exposed)    |
| Li & Zhang (2022)           | (not exposed)         | `Li22_pss`                                                               | (not exposed)    |

SulfurX hardcodes Smythe et al. (2017) as `SCSS_smythe()` in the upstream `Sulfur_Saturation` class (read in [`backends/sulfurx.py`](../src/volcatenate/backends/sulfurx.py)); volcatenate does not expose a YAML override. To match SulfurX in VolFe, set `volfe.scss = "Smythe17_pss"`; in MAGEC, `magec.scss = 3`. EVo cannot be matched today.

### SCAS — sulfate saturation models

| Model family                | VolFe (`scas`)           | EVo, MAGEC, SulfurX                                                                                                                                                                                                                             |
| --------------------------- | -------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Chowdhury & Dasgupta (2019) | `Chowdhury19_pss`        | (not exposed)                                                                                                                                                                                                                                   |
| Zajacz & Tsay (2019)        | `Zajacz19_pss` (default) | EVo: not exposed (no SCAS handling at all). MAGEC: not exposed (sulfate saturation toggled via `sulfate_sat = 1`, but the SCAS model is selected internally). SulfurX: hardcoded as `SCAS_Zajacz_Tsay()` in upstream `Sulfur_Saturation`. |
| Masotta & Keppler (2015)    | `Masotta15_pss`          | (not exposed)                                                                                                                                                                                                                                   |
| Liu et al. (2023)           | `Liu23`                  | (not exposed)                                                                                                                                                                                                                                   |

VolFe is the only backend with a YAML choice of SCAS model. SulfurX hardcodes Zajacz-Tsay 2019, which happens to match VolFe's default — so the two are like-for-like out of the box.

### Saturation enforcement — when each backend clips or terminates

| Backend | Default behavior                                                  | YAML key to enable enforcement                                                        | What "enforce" means upstream                                                                                                                                                                                                                                                                             |
| ------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| EVo     | **No saturation check at all** (`s_sat_warn = false`)     | `evo.s_sat_warn = true`                                                             | When `s_sat_warn = true` and gas system is `SOH` / `COHS` / `COHSN`, EVo computes SCSS at every step and **terminates the run** if S > SCSS or if S6+/(S²⁻+S⁶⁺) ≥ 0.1. The standardized output therefore stops at the saturation pressure rather than continuing with a sulfide phase. |
| VolFe   | **No saturation check** (`sulfur_saturation = false`)     | `volfe.sulfur_saturation = true`                                                    | When true, VolFe checks SCSS / SCAS at every pressure step and**clips** dissolved S to the saturation curve, treating excess S as a separate sulfide / sulfate phase. The degassing path continues normally past saturation.                                                                        |
| MAGEC   | **No saturation check** (`sulfide_sat = sulfate_sat = 0`) | `magec.sulfide_sat = 1` (and / or `magec.sulfate_sat = 1`) — independent toggles | When 1, MAGEC's solver consults SCSS / SCAS at every pressure step and clips melt S to the saturation curve, similar to VolFe.                                                                                                                                                                            |
| SulfurX | **No saturation check** (`sulfide_pre = 0`)               | `sulfurx.sulfide_pre = 1`                                                           | When 1, SulfurX checks SCSS at every step and precipitates a sulfide phase (clipping melt S); the per-step output reports `sulfide_frac` as the mass fraction precipitated. SCAS handling is internal regardless of this toggle.                                                                        |

This is the single biggest source of "why don't the four backends agree" in S behavior. By default, *none* of the four backends check sulfide saturation. For a like-for-like comparison that includes saturation, set `evo.s_sat_warn = true`, `volfe.sulfur_saturation = true`, `magec.sulfide_sat = 1` (and `magec.sulfate_sat = 1` if needed), and `sulfurx.sulfide_pre = 1` — and be aware that EVo will *terminate* the run at saturation rather than continuing with a sulfide phase the way the other three do.

### Cross-backend recipe — O'Neill family across all four backends

A common comparison setup is to run the four backends with O'Neill 2021 sulfide capacity / SCSS and O'Neill & Mavrogenes 2022 sulfate equilibrium / S6+/ST. The closest YAML configuration to that intent:

```yaml
evo:
  sulfide_capacity: oneill2020   # O'Neill (2020) — paper-of-record before the 2021 chapter
  sulfate_capacity: nash2019     # EVo does not expose ONeill22 sulfate-capacity; default Nash19 stands
  scss: liu2007                  # EVo does not expose ONeill21 SCSS; default Liu07 stands
  s_sat_warn: true               # turns on the saturation check (and run-termination at saturation)

volfe:
  sulfide_sol: ONeill21dil       # O'Neill (2021) inc. H2O dilution
  sulfate_sol: ONeill22dil       # O'Neill & Mavrogenes (2022) inc. H2O dilution
  scss: ONeill21_pss             # O'Neill (2021) via PySulfSat
  scas: Zajacz19_pss             # closest cross-backend match; no ONeill SCAS exposed
  sulfur_saturation: true        # required for SCSS / SCAS to be consulted

magec:
  sulfide_cap: 2                 # O'Neill (2021)
  s_redox: 4                     # O'Neill & Mavrogenes (2022)
  scss: 4                        # O'Neill (2021)
  sulfide_sat: 1
  sulfate_sat: 1

sulfurx:
  s_fe_choice: 1                 # O'Neill & Mavrogenes (2022)
  sulfide_pre: 1                 # required for SCSS to trigger
  # SulfurX uses Smythe17 SCSS and Zajacz-Tsay 2019 SCAS internally; no YAML override.
```

EVo has the fewest cross-backend matches in the O'Neill / ONeill22 family — only `sulfide_capacity` is in that lineage; SCSS and sulfate-capacity choices stay at their volcatenate defaults. EVo's run will additionally terminate at sulfide saturation (because of `s_sat_warn = true`) where VolFe / MAGEC / SulfurX continue with a sulfide phase, so EVo's standardized output will be shorter past the saturation pressure.

---

## Quick Cross-reference

How to set specific run parameters across all back-ends, either with the YAML configuration or in the sample composition (passed as MeltComposition class or from a csv file).

### Closed-system degassing

| Backend | YAML setting                            |
| ------- | --------------------------------------- |
| VESIcal | `vesical.fractionate_vapor: 0.0`      |
| VolFe   | `volfe.gassing_style: closed`         |
| EVo     | `evo.run_type: closed` (default)      |
| MAGEC   | always closed (no toggle exposed)       |
| SulfurX | `sulfurx.open_degassing: 0` (default) |

### Open-system degassing

| Backend | YAML setting                                       |
| ------- | -------------------------------------------------- |
| VESIcal | `vesical.fractionate_vapor: 1.0`                 |
| VolFe   | `volfe.gassing_style: open`                      |
| EVo     | `evo.run_type: open` AND `evo.loss_frac < 1.0` |
| MAGEC   | not currently exposed                              |
| SulfurX | `sulfurx.open_degassing: 1`                      |

### Set specific dFMQ value

| Backend | What you need on the sample                                                                                       | What the wrapper sends                                                                                          |
| ------- | ----------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| VESIcal | (no fO2 input)                                                                                                    | n/a                                                                                                             |
| VolFe   | `dFMQ` and `volfe.fo2_column: DFMQ`                                                                           | `DFMQ` column                                                                                                 |
| EVo     | `dFMQ` (auto-picked when `fo2_source="auto"`)                                                                 | `FO2_buffer=FMQ`, offset = sample dFMQ                                                                        |
| MAGEC   | `dFMQ` and `magec.redox_option: dFMQ`. Use `magec.redox_source: dfmq` to refuse the silent KC91 conversion. | `dFMQ` directly, **or** `Fe3+/FeT` via KC91 if `redox_source="auto"` and Fe3+/FeT was used instead. |
| SulfurX | `dFMQ` (always preferred when present)                                                                          | `delta_FMQ`                                                                                                   |

### Solubility model choice

Each backend has its own solubility model settings:

| Volatile        | VolFe                 | EVo                      | MAGEC                 | SulfurX                                 |
| --------------- | --------------------- | ------------------------ | --------------------- | --------------------------------------- |
| H2O             | `volfe.h2o_sol`     | `evo.h2o_model`        | `magec.h2o_sol`     | `sulfurx.coh_model` (paired with CO2) |
| CO2             | `volfe.co2_sol`     | `evo.c_model`          | `magec.co2_sol`     | `sulfurx.coh_model`                   |
| H2              | `volfe.h2_sol`      | `evo.h2_model`         | (not exposed)         | n/a                                     |
| CO              | `volfe.co_sol`      | `evo.co_model`         | `magec.co_sol`      | n/a                                     |
| CH4             | `volfe.ch4_sol`     | `evo.ch4_model`        | (not exposed)         | n/a                                     |
| Sulfide (S²⁻) | `volfe.sulfide_sol` | `evo.sulfide_capacity` | `magec.sulfide_cap` | (built-in)                              |
| Sulfate (S⁶⁺) | `volfe.sulfate_sol` | `evo.sulfate_capacity` | (built-in)            | (built-in)                              |
| H2S             | `volfe.h2s_sol`     | (built-in)               | (built-in)            | (built-in)                              |

VESIcal is omitted (no sulfur, and only one solubility law per variant).

---

## Where To Look Next

- [`configuration.md`](configuration.md) — top-level YAML schema and loading.
- [`run_bundles.md`](run_bundles.md) — reproducible run bundles (which include the resolved config so you can replay).
- The wrapper modules under `src/volcatenate/backends/` — read these whenever the doc above is unclear; they are short and are the source of truth.
- The project's internal config audit — full list of every hardcoded literal, every silent fallback, and proposed promotions for upcoming releases (kept alongside the repo, not published).
